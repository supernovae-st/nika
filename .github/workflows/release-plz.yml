# release-plz workflow
# Automates version bumping, CHANGELOG generation, and release PRs
#
# Flow:
#   push to main → validates → creates/updates Release PR
#   merge Release PR → creates git tag → triggers release.yml
#
# Documentation: https://release-plz.ieni.dev/docs/github

name: Release-plz

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release:
        description: 'Create release (tag + publish)'
        required: false
        type: boolean
        default: false

permissions:
  pull-requests: write
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # =============================================================================
  # Validation - Run before creating release PR
  # =============================================================================
  validate:
    name: Pre-release Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tools/nika

      - name: Check formatting
        run: cargo fmt --all --check --manifest-path tools/nika/Cargo.toml

      - name: Run clippy
        run: cargo clippy --all-targets --all-features --manifest-path tools/nika/Cargo.toml -- -D warnings

      - name: Install cargo-nextest
        run: curl -LsSf https://get.nexte.st/latest/linux | tar zxf - -C ${CARGO_HOME:-~/.cargo}/bin

      - name: Run tests
        run: cargo nextest run --workspace --lib --manifest-path tools/Cargo.toml

      - name: Install cargo-semver-checks
        run: cargo install cargo-semver-checks --locked

      - name: Check semver compatibility
        run: cargo semver-checks check-release --manifest-path tools/nika/Cargo.toml
        continue-on-error: true

  # =============================================================================
  # Release PR - Creates/updates PR with version bumps and CHANGELOG
  # =============================================================================
  release-plz-pr:
    name: Create Release PR
    needs: validate
    runs-on: ubuntu-latest
    # Only run on push to main, not on workflow_dispatch with release=true
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && !inputs.release)
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run release-plz
        uses: MarcoIeni/release-plz-action@v0.5
        with:
          command: release-pr
          manifest_path: tools/nika/Cargo.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # =============================================================================
  # Release - Triggered when release PR is merged (or manual dispatch)
  # =============================================================================
  release-plz-release:
    name: Release
    runs-on: ubuntu-latest
    # Only run on manual dispatch with release=true
    if: github.event_name == 'workflow_dispatch' && inputs.release
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run release-plz release
        uses: MarcoIeni/release-plz-action@v0.5
        with:
          command: release
          manifest_path: tools/nika/Cargo.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
