[workspace]
resolver = "3"
members  = ["tools/nika-error", "tools/nika-catalog", "tools/nika-kernel", "tools/nika-kernel-mock"]
# Excluded so cargo does not try to descend into untracked main-leftover
# manifests under tools/ (the orphan working tree carries them but they
# are not part of the diamond workspace).
exclude = [
  "tools/nika-blob", "tools/nika-builtin", "tools/nika-cli", "tools/nika-clock",
  "tools/nika-core", "tools/nika-daemon", "tools/nika-display", "tools/nika-engine",
  "tools/nika-event", "tools/nika-exec-runner", "tools/nika-extract", "tools/nika-fs",
  "tools/nika-http", "tools/nika-init",
  "tools/nika-lsp", "tools/nika-lsp-core", "tools/nika-macros", "tools/nika-mcp",
  "tools/nika-media", "tools/nika-policy", "tools/nika-sdk", "tools/nika-security",
  "tools/nika-serve", "tools/nika-storage", "tools/nika-vault",
  "tools/nika-verb-exec", "tools/nika-verb-fetch", "tools/nika-verb-infer",
  "tools/nika-verb-invoke", "tools/nika",
]

[workspace.package]
version      = "0.80.0"
edition      = "2024"
license      = "AGPL-3.0-or-later"
authors      = ["Thibaut Melen <thibaut@supernovae.studio>"]
rust-version = "1.91"
repository   = "https://github.com/supernovae-st/nika"
homepage     = "https://supernovae.studio"

# ─────────────────────────────────────────────────────────────────────────────
# Workspace-wide lint policy. Every member crate MUST opt in via:
#     [lints]
#     workspace = true
# ─────────────────────────────────────────────────────────────────────────────

[workspace.lints.rust]
unsafe_code                    = "forbid"
unused_must_use                = "deny"
elided_lifetimes_in_paths      = "warn"
unreachable_pub                = "warn"
explicit_outlives_requirements = "warn"
unused_extern_crates           = "warn"

[workspace.lints.clippy]
# Group levels (priority = -1 so individual lints below win)
correctness = { level = "deny", priority = -1 }
suspicious  = { level = "warn", priority = -1 }
perf        = { level = "deny", priority = -1 }
pedantic    = { level = "warn", priority = -1 }

# Core "diamond" lints — zero tolerance on production src/
unwrap_used   = "deny"
expect_used   = "warn"
panic         = "deny"
todo          = "warn"
unimplemented = "warn"
dbg_macro     = "deny"
print_stdout  = "warn"
print_stderr  = "warn"

# ─────────────────────────────────────────────────────────────────────────────
# Shared dependencies. Populated when Phase 1 extract-ready crates land.
# Rule (RUST_ENFORCEMENT §2): pin once here, downstream = `.workspace = true`.
# ─────────────────────────────────────────────────────────────────────────────

[workspace.dependencies]
# Pin once here, downstream uses `.workspace = true` (RUST_ENFORCEMENT §2).
thiserror  = "2.0"
miette     = "7.6"
serde      = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
phf        = { version = "0.13", features = ["macros", "unicase"] }
unicase    = "2.9"

trait-variant = "0.1"
bytes         = "1"
futures-core  = "0.3"
parking_lot   = "0.12"
tokio         = { version = "1", features = ["rt", "macros", "time"] }

# Forward-pinned shared deps (used by Phase 1+ crates).
# Pinning here keeps versions aligned when downstream crates opt in via
# `.workspace = true`. Per RUST_ENFORCEMENT §2.
strsim   = "0.11"   # nika-error Phase C suggest() — Levenshtein for "did you mean"
smallvec = "1.13"   # SmallVec adoption per RUST_2026_BEST_PRACTICES (hot-path vecs)
tracing  = "0.1"    # L1+ effect crates (fs/http/process/...) structured logging

# Dev / test deps
proptest = "1.6"
insta    = { version = "1.42", features = ["yaml"] }
rstest   = "0.22"
