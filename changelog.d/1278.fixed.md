The check cost table distinguishes unpriced catalogued cloud models from local
models and unknown provider/model names. The `run --dry-run` help explicitly
names its budget exemption; previewing does not judge `--max-cost-usd`.
