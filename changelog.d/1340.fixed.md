- **Release visibility barrier.** New release trains freeze and recheck one
  peeled tag commit, converge immutable GitHub/npm/GHCR identities behind a
  draft, verify the exact eight assets plus five source-bound attestations,
  and let one finalizer publish before stable Homebrew/`latest` pointers move.
