The resident's public `/health` response now reports `storeFormatVersion` for jobs and schedules from the stores' own version constants. A frozen jobs v3 fixture pins queued and settled records, receipts, event hashes, the writer stamp and incarnation without rewriting evidence on read.
