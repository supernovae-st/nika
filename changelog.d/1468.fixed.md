Named HTTP admission now has deterministic route-level proofs for file and directory symlink replacement between lookup and capture. Both refuse before creating a job or entering the backend; receipts retain the exact captured snapshot digest when regular files change before or after capture.
