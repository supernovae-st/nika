Model overrides now revalidate workflow composition and MCP server configuration on both check and run, refusing missing servers instead of returning a successful check report without execution. Human MCP diagnostics retain the same registry explanation as JSON.
