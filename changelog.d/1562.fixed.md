- **Unknown command-form panics carry no command payload.** The forward-compatibility refusal in static argv analysis keeps its failure signal without formatting command data into panic output. Both current command variants remain handled explicitly.
