- **A value read of a skipped confirm stage is a `NIKA-SEC-014` refusal.**
  A refused confirm settles `success`/`false`; the stage gated on the answer
  then settles `skipped`, and a value edge admits the skip (the binding reads
  defined-null), so a task that only read the stage's output still reached
  its verb on « no » under a clean `nika check`. The consent walk now
  continues past a closed gate over the value-carrying edges and judges each
  task with two readings of one GATE-v2 fold: provably cancelled in every
  refusal run is silence (a same-producer `after: { stage: success }`
  cancels the reader), provably reaching its verb in every refusal run is
  the refusal (the finding names the skipped stage), anything between — a
  laundering hop, an independent `after: { x: success }` prerequisite, a
  `when:` that may error before it decides — stays the advisory hint: no
  verb is ever assumed to succeed. Routes that cross no closed gate keep
  their verdicts and wording unchanged.
