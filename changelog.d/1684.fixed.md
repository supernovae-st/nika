- **Project schemas follow canonical program names.** The JSON Schema accepts
  `.nika` workflow references and preserves the project parser's relative-path
  restrictions. Source paths reject control bytes in parent components as well
  as basenames. Pack synchronization prunes excluded repository READMEs instead
  of embedding stale navigation.
