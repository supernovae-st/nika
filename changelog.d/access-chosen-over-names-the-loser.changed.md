- **« chosen over … » names the seat it beat.** The `nika check` ACCESS rung and the `nika run` announce list the outranked ready paths with their witness (`chosen over api (key present)`) instead of a bare count, so a user learns which seat lost without opening the JSON.
