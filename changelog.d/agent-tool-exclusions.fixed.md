Agent tool exclusions such as `!nika:done` no longer produce a spurious
permission refusal during static checks or runtime dispatch, or appear as
required grants in inferred permissions and task capability projections.
Positive tool grants retain their boundary checks.
