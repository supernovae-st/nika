Recognize the default thinking mode of the exact DeepSeek `deepseek-v4-pro`
and `deepseek-flash` API models. Generated language steps now use the existing
reasoning token floor, leaving room for the structured answer instead of
applying the smaller plain-chat budget. Legacy aliases and gateway routes keep
their separate capability and cost-admission rules.
