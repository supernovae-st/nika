- **`NIKA-PARSE-024` promises `nika check --fix` only for the shape the migrator proves.** A `depends_on:` whose entries are all bare task ids is migrated; a scalar, a map entry or any other string is named as the author's to rewrite, the `after:` example never splices a non-id, and `nika explain NIKA-PARSE-024` says which shapes migrate — the finding and the fixer no longer contradict each other on one screen (wave 3 · persona 02).
