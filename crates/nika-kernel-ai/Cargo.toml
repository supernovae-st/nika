[package]
name         = "nika-kernel-ai"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
publish      = false  # Foundation crate — never on crates.io. See feedback_publish_false_foundation_strategy.md + ADR-017.
description  = "AI sibling of the split kernel — provider, memory, vision, context trait contracts — L0.5"

[package.metadata.cargo-machete]
ignored = ["insta"]  # dev-dep used in #[cfg(test)] blocks

[lints]
workspace = true

[dependencies]
nika-error       = { path = "../nika-error", version = "0.116.1" }
nika-kernel-core = { path = "../nika-kernel-core", version = "0.116.1" }
trait-variant = { workspace = true }
thiserror     = { workspace = true }
miette        = { workspace = true }
serde         = { workspace = true }
serde_json    = { workspace = true }
bytes         = { workspace = true }
futures-core  = { workspace = true }
uuid          = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
tokio = { workspace = true }
