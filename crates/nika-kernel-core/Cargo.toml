[package]
name         = "nika-kernel-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
publish      = false  # Foundation crate — never on crates.io. See feedback_publish_false_foundation_strategy.md + ADR-017.
description  = "Base sibling of the split kernel — io/infra trait contracts + cancel/sealed/types primitives — L0.5"

[package.metadata.cargo-machete]
ignored = ["insta"]  # dev-dep used in #[cfg(test)] blocks

[lints]
workspace = true

[dependencies]
nika-error    = { path = "../nika-error", version = "0.112.0" }
nika-cap      = { path = "../nika-cap", version = "0.112.0" }     # the env plane of the capability boundary (NEP-0005 · DANGEROUS/RUNNER floors + compose_child_env · re-exported at the historical process path)
trait-variant = { workspace = true }
thiserror     = { workspace = true }
miette        = { workspace = true }
serde         = { workspace = true }
serde_json    = { workspace = true }
bytes         = { workspace = true }
futures-core  = { workspace = true }
zeroize       = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
tokio = { workspace = true }
