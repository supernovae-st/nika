[package]
name         = "nika-kernel-plugin"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
publish      = false  # Foundation crate — never on crates.io. See feedback_publish_false_foundation_strategy.md + ADR-017.
description  = "Plugin sibling of the split kernel — WASM host + sandbox trait contracts (ADR-020) — L0.5"

[package.metadata.cargo-machete]
ignored = ["insta"]  # dev-dep used in #[cfg(test)] blocks

[lints]
workspace = true

[dependencies]
nika-error       = { path = "../nika-error", version = "0.117.0" }
nika-kernel-core = { path = "../nika-kernel-core", version = "0.117.0" }
trait-variant = { workspace = true }
thiserror     = { workspace = true }
miette        = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
