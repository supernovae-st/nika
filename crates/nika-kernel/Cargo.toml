[package]
name         = "nika-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
publish      = false  # Foundation crate — never on crates.io. See feedback_publish_false_foundation_strategy.md + ADR-017.
description  = "Trait contracts for every side effect in the Nika diamond — L0.5"

[lints]
workspace = true

[dependencies]
nika-error          = { path = "../nika-error", version = "0.113.0" }
nika-kernel-ai      = { path = "../nika-kernel-ai", version = "0.113.0" }
nika-kernel-core    = { path = "../nika-kernel-core", version = "0.113.0" }
nika-kernel-plugin  = { path = "../nika-kernel-plugin", version = "0.113.0" }
nika-kernel-runtime = { path = "../nika-kernel-runtime", version = "0.113.0" }

[dev-dependencies]
serde_json = { workspace = true }
