# TaskBox Enhancement v0.11 - Complete Implementation Plan

**Version:** v1.0
**Date:** 2026-02-26
**Authors:** 6 specialized agents (Architecture, Code Review, Rust Expert)
**Target:** Nika v0.11.0

---

## Executive Summary

This document consolidates implementation plans for TaskBox visual enhancements across 17 phases. All plans follow TDD methodology and use the existing VerbColor/Theme system.

**Total Effort:** ~40 hours (5-6 days)
**Total New Tests:** 91 tests
**Files Modified:** 15 files

---

## Phase Overview

### InferBox Enhancements (Phases 1-4)

| Phase | Feature | Tests | Effort |
|-------|---------|-------|--------|
| 1 | Thinking Capture Display | 5 | 2h |
| 2 | Model Badge Enhancement | 5 | 1.5h |
| 3 | Token Velocity Widget | 3 | 1h |
| 4 | Streaming Progress Indicator | 6 | 2h |
| **Subtotal** | | **19 tests** | **6.5h** |

### ExecBox/FetchBox Enhancements (Phases 6-9)

| Phase | Feature | Tests | Effort |
|-------|---------|-------|--------|
| 6 | ExecBox Compact Mode | 5 | 3h |
| 7 | ExecBox Streaming Progress | 3 | 2h |
| 8 | FetchBox HTTP Method Badges | 4 | 2h |
| 9 | FetchBox Response Preview | 4 | 3h |
| **Subtotal** | | **16 tests** | **10h** |

### InvokeBox Enhancements (Phases 10-12)

| Phase | Feature | Tests | Effort |
|-------|---------|-------|--------|
| 10 | Tool Icon Mapping System | 4 | 1h |
| 11 | Param Preview Enhancement | 5 | 2h |
| 12 | Retry Indicator Badge | 4 | 1h |
| **Subtotal** | | **13 tests** | **4h** |

### TaskBox Wiring (Phases 13-17)

| Phase | Feature | Tests | Effort |
|-------|---------|-------|--------|
| 13 | TaskStarted Event Handler | 3 | 3h |
| 14 | Theme-Aware BoxState | 2 | 2h |
| 15 | Animation Synchronization | 2 | 4h |
| 16 | TokenVelocity Wiring | 2 | 3h |
| 17 | AgentSpawned Visualization | 3 | 5h |
| **Subtotal** | | **12 tests** | **17h** |

### Code Quality Improvements

| Item | Tests | Effort |
|------|-------|--------|
| ExecBox truncate() edge cases | 5 | 1h |
| ExecBox render safety tests | 4 | 1h |
| ExecBox exit_display() Cow | 1 | 30m |
| ExecBox allocation caching | 2 | 2h |
| **Subtotal** | **12 tests** | **4.5h** |

---

## Architecture Context

### Theme System (theme.rs)

```rust
pub enum VerbColor {
    Infer,   // Violet #8b5cf6 - ⚡
    Exec,    // Amber #f59e0b - 📟
    Fetch,   // Cyan #06b6d4 - 🛰️
    Invoke,  // Emerald #10b981 - 🔌
    Agent,   // Rose #f43f5e - 🐔
    Spawn,   // Rose 300 #fda4af - 🐤
}

impl VerbColor {
    pub fn rgb(&self) -> Color;      // ratatui::Color
    pub fn glow(&self) -> Color;     // Brightened variant
    pub fn muted(&self) -> Color;    // Dimmed variant
    pub fn subtle(&self) -> Color;   // Very dim variant
    pub fn icon(&self) -> &'static str;
    pub fn label(&self) -> &'static str;
    pub fn icon_label(&self) -> String; // "⚡ INFER"
}
```

### BoxState (state.rs)

```rust
pub enum BoxState {
    Queued,
    Running { start: Instant, spinner_frame: u8 },
    Success { duration: Duration },
    Failed { error: String, duration: Duration },
    Skipped { reason: String },
}

impl BoxState {
    pub fn border_color_with_pulse(&self, verb: Color, intensity: f32) -> Color;
    pub fn icon(&self) -> &'static str;  // State icons (⬜, ⏳, ✅, ❌, ⏭)
    pub fn suffix(&self) -> Cow<'_, str>; // "0.3s", "Error: ..."
}
```

### Animation System (animation.rs)

```rust
pub struct AnimationTicker {
    frame: u64,
    started: Instant,
}

impl AnimationTicker {
    pub fn tick(&mut self) -> u16;  // Returns frame % 8
    pub fn pulse(&self) -> f32;     // 0.0-1.0 sine wave
}

pub enum Easing {
    Linear,
    EaseIn,
    EaseOut,
    EaseInOut,
    Elastic,  // v0.10 - overshoots then settles
    Spring,   // v0.10 - oscillating settle
}
```

---

## Phase 1: Thinking Capture Display

**File:** `src/tui/widgets/task_box/infer.rs`

### Changes

1. **Add fields (after line 45):**
```rust
pub thinking_content: Option<String>,
pub expanded_thinking: bool,
```

2. **Add builder method:**
```rust
pub fn with_thinking_text(mut self, thinking: impl Into<String>) -> Self {
    self.thinking_content = Some(thinking.into());
    self
}

pub fn toggle_thinking(&mut self) {
    self.expanded_thinking = !self.expanded_thinking;
}
```

3. **Update required_height():**
```rust
if self.thinking_content.is_some() {
    height += 2; // Section header + separator
    if self.expanded_thinking {
        height += self.thinking_content.as_ref().unwrap()
            .lines().count().min(15) as u16;
    } else {
        height += 1; // Collapsed preview
    }
}
```

4. **Render thinking section (after prompt, before response):**
```rust
if let Some(ref thinking) = self.thinking_content {
    // Separator
    let sep = format!("├{}┤", "─".repeat(inner_width));
    buf.set_string(area.x, y, &sep, border_style);
    y += 1;

    // Header: 💭 THINKING ▼
    let thinking_icon = if self.expanded_thinking { "▼" } else { "▶" };
    buf.set_string(area.x + 2, y,
        format!("💭 THINKING {}", thinking_icon),
        Style::default().fg(VerbColor::Infer.muted()));
    y += 1;

    // Content
    let display = if self.expanded_thinking {
        thinking.clone()
    } else {
        Self::truncate(&thinking.replace('\n', " "), inner_width - 4)
    };
    buf.set_string(area.x + 2, y, format!("┊ {}", display),
        Style::default().fg(VerbColor::Infer.subtle()));
}
```

### Tests

```rust
#[test]
fn test_infer_box_with_thinking_text() {
    let box_ = InferBox::new("claude-sonnet-4", "prompt")
        .with_thinking_text("Let me analyze...");
    assert!(box_.thinking_content.is_some());
    assert!(!box_.expanded_thinking);
}

#[test]
fn test_infer_box_toggle_thinking() {
    let mut box_ = InferBox::new("model", "prompt")
        .with_thinking_text("Analysis");
    assert!(!box_.expanded_thinking);
    box_.toggle_thinking();
    assert!(box_.expanded_thinking);
}

#[test]
fn test_infer_box_thinking_increases_height() {
    let no_thinking = InferBox::new("model", "prompt");
    let with_thinking = InferBox::new("model", "prompt")
        .with_thinking_text("Content");
    assert!(with_thinking.required_height() > no_thinking.required_height());
}

#[test]
fn test_infer_box_thinking_expanded_height() {
    let mut box_ = InferBox::new("model", "prompt")
        .with_thinking_text("Line 1\nLine 2\nLine 3");
    let collapsed = box_.required_height();
    box_.expanded_thinking = true;
    assert!(box_.required_height() > collapsed);
}

#[test]
fn test_infer_box_thinking_none_no_render() {
    let box_ = InferBox::new("model", "prompt");
    assert!(box_.thinking_content.is_none());
}
```

---

## Phase 2: Model Badge Enhancement

**File:** `src/tui/widgets/task_box/infer.rs`

### Changes

1. **Replace provider_icon() with provider_badge():**
```rust
fn provider_badge(model: &str) -> &'static str {
    if model.contains("claude-sonnet") { "Claude Sonnet" }
    else if model.contains("claude-opus") { "Claude Opus" }
    else if model.contains("claude") { "Claude" }
    else if model.contains("gpt-4o") { "GPT-4o" }
    else if model.contains("gpt-4") { "GPT-4" }
    else if model.contains("gpt-3.5") { "GPT-3.5" }
    else if model.contains("mistral-large") { "Mistral Large" }
    else if model.contains("mistral") { "Mistral" }
    else if model.contains("llama") { "Llama" }
    else if model.contains("deepseek") { "DeepSeek" }
    else if model.contains("groq") { "Groq" }
    else { "LLM" }
}
```

2. **Update model line rendering:**
```rust
let badge = Self::provider_badge(&self.model);
let model_display = Self::truncate(&self.model, inner_width - badge.len() - 12);
buf.set_string(area.x + 2, y,
    format!("model: [{}] {}", badge, model_display),
    dim_style);
```

### Tests

```rust
#[test]
fn test_provider_badge_claude_sonnet() {
    assert_eq!(InferBox::provider_badge("claude-sonnet-4-6"), "Claude Sonnet");
}

#[test]
fn test_provider_badge_gpt4() {
    assert_eq!(InferBox::provider_badge("gpt-4o"), "GPT-4o");
    assert_eq!(InferBox::provider_badge("gpt-4-turbo"), "GPT-4");
}

#[test]
fn test_provider_badge_mistral() {
    assert_eq!(InferBox::provider_badge("mistral-large-latest"), "Mistral Large");
}

#[test]
fn test_provider_badge_other() {
    assert_eq!(InferBox::provider_badge("llama-3.2"), "Llama");
    assert_eq!(InferBox::provider_badge("deepseek-chat"), "DeepSeek");
}

#[test]
fn test_provider_badge_unknown() {
    assert_eq!(InferBox::provider_badge("unknown-model"), "LLM");
}
```

---

## Phase 3: Token Velocity Widget

**Note:** Token velocity is already wired but needs verification.

**File:** `src/tui/widgets/task_box/infer.rs` (lines 356-363)

### Verification

The velocity display code already exists:
```rust
let velocity_str = if self.state.is_running() && !self.velocity.is_empty() {
    format!(" │ {} {:.0} tok/s",
        self.velocity.sparkline_chars(),
        self.velocity.average())
} else {
    String::new()
};
```

### Tests

```rust
#[test]
fn test_infer_box_velocity_renders_when_running() {
    let mut box_ = InferBox::new("claude", "prompt")
        .with_state(BoxState::running());
    box_.push_velocity_sample(45.0);
    box_.push_velocity_sample(52.0);
    assert!(!box_.velocity.is_empty());
    assert!(box_.state.is_running());
}

#[test]
fn test_infer_box_velocity_hidden_when_not_running() {
    let mut box_ = InferBox::new("model", "prompt")
        .with_state(BoxState::success(1000));
    box_.push_velocity_sample(50.0);
    assert!(!box_.velocity.is_empty());
    assert!(!box_.state.is_running());
}

#[test]
fn test_infer_box_velocity_empty_no_render() {
    let box_ = InferBox::new("model", "prompt")
        .with_state(BoxState::running());
    assert!(box_.velocity.is_empty());
}
```

---

## Phase 4: Streaming Progress Indicator

**File:** `src/tui/widgets/task_box/infer.rs`

### Changes

1. **Add progress bar helper:**
```rust
fn render_progress_bar(progress: f32, width: usize) -> String {
    let progress = progress.clamp(0.0, 1.0);
    let bar_width = width.saturating_sub(6);
    let filled = (progress * bar_width as f32) as usize;
    format!("[{}{}] {:>3.0}%",
        "█".repeat(filled),
        " ".repeat(bar_width.saturating_sub(filled)),
        progress * 100.0)
}
```

2. **Update response header:**
```rust
let response_header = if self.streaming_cursor && self.state.is_running() {
    let progress = (self.tokens_out as f32) / 2000.0; // Assume 2000 target
    let progress_bar = Self::render_progress_bar(progress, inner_width - 15);
    format!("RESPONSE {} streaming...", progress_bar)
} else if !self.response.is_empty() {
    format!("RESPONSE ▼ {} chars", self.response.len())
} else {
    "RESPONSE".to_string()
};
```

### Tests

```rust
#[test]
fn test_render_progress_bar_zero() {
    let bar = InferBox::render_progress_bar(0.0, 20);
    assert!(bar.contains("  0%"));
}

#[test]
fn test_render_progress_bar_half() {
    let bar = InferBox::render_progress_bar(0.5, 20);
    assert!(bar.contains("50%"));
    assert!(bar.contains('█'));
}

#[test]
fn test_render_progress_bar_full() {
    let bar = InferBox::render_progress_bar(1.0, 20);
    assert!(bar.contains("100%"));
}

#[test]
fn test_render_progress_bar_clamped() {
    let over = InferBox::render_progress_bar(1.5, 20);
    assert!(over.contains("100%"));
    let under = InferBox::render_progress_bar(-0.5, 20);
    assert!(under.contains("  0%"));
}

#[test]
fn test_infer_box_streaming_progress_shown() {
    let box_ = InferBox::new("claude", "prompt")
        .with_state(BoxState::running())
        .with_streaming_cursor(true)
        .with_tokens(100, 250);
    assert!(box_.streaming_cursor);
    assert!(box_.state.is_running());
}

#[test]
fn test_infer_box_no_progress_when_not_streaming() {
    let box_ = InferBox::new("model", "prompt")
        .with_state(BoxState::success(1000));
    assert!(!box_.streaming_cursor);
}
```

---

## Phase 6: ExecBox Compact Mode

**File:** `src/tui/widgets/task_box/exec.rs`

### Changes

1. **Add render_compact() method:**
```rust
fn render_compact(&self, area: Rect, buf: &mut Buffer) {
    let verb_color = VerbColor::Exec.rgb();
    let status_badge = match (&self.exit_code, &self.state) {
        (Some(0), _) => Span::styled(" ✓ ", Style::default()
            .fg(Color::Green).add_modifier(Modifier::BOLD)),
        (Some(code), _) => Span::styled(format!(" ✗ {} ", code),
            Style::default().fg(exit::code_color(*code))),
        (None, BoxState::Running { .. }) => Span::styled(" ⋯ ",
            Style::default().fg(verb_color)),
        _ => Span::styled(" ⋯ ", Style::default().fg(Color::Gray)),
    };

    let command = Self::truncate(&self.command, 60);
    let line = Line::from(vec![
        Span::styled("📟 ", Style::default().fg(verb_color)),
        Span::styled(command, Style::default().fg(Color::White)),
        status_badge,
    ]);
    buf.set_line(area.x, area.y, &line, area.width);
}
```

2. **Update Widget::render() to dispatch by mode:**
```rust
fn render(self, area: Rect, buf: &mut Buffer) {
    match self.render_mode {
        RenderMode::Compact => self.render_compact(area, buf),
        RenderMode::Expanded => self.render_expanded(area, buf),
        RenderMode::Full => self.render_full(area, buf),
    }
}
```

### Tests

```rust
#[test]
fn test_exec_compact_mode_single_line() {
    let exec = ExecBox::new("npm test").with_exit_code(0);
    assert_eq!(exec.required_height_for_mode(RenderMode::Compact), 1);
}

#[test]
fn test_exec_compact_exit_nonzero_shows_error() {
    let exec = ExecBox::new("cargo build").with_exit_code(1);
    // Exit code 1 should show red badge
}

#[test]
fn test_exec_compact_running_shows_spinner() {
    let exec = ExecBox::new("sleep 10").with_state(BoxState::running());
    // Running state should show ⋯
}

#[test]
fn test_exec_expanded_shows_output_without_metadata() {
    let exec = ExecBox::new("echo test")
        .with_stdout("test\n")
        .with_exit_code(0);
    // Expanded should show output but not full metadata
}

#[test]
fn test_exec_compact_truncates_long_commands() {
    let long_cmd = "npm run build --production --minify --sourcemap --verbose";
    let exec = ExecBox::new(long_cmd).with_exit_code(0);
    // Command should be truncated with ellipsis
}
```

---

## Phase 7: ExecBox Streaming Progress

**File:** `src/tui/widgets/task_box/exec.rs`

### Changes

1. **Add progress bar for running commands:**
```rust
fn render_progress_bar(&self, duration: Duration) -> Line<'static> {
    const MAX_DURATION_SECS: f32 = 60.0;
    const BAR_WIDTH: usize = 20;

    let progress = (duration.as_secs_f32() / MAX_DURATION_SECS).min(1.0);
    let filled = (progress * BAR_WIDTH as f32) as usize;

    Line::from(vec![
        Span::raw("  "),
        Span::styled("█".repeat(filled),
            Style::default().fg(VerbColor::Exec.rgb())),
        Span::styled("░".repeat(BAR_WIDTH - filled),
            Style::default().fg(Color::DarkGray)),
        Span::raw(format!(" {:.1}s", duration.as_secs_f32())),
    ])
}
```

2. **Add tick() method for animation:**
```rust
pub fn tick(&mut self) {
    if self.exit_code.is_none() {
        self.running_since = Some(self.started_at.elapsed());
    }
}
```

### Tests

```rust
#[test]
fn test_exec_streaming_shows_progress_bar() {
    let exec = ExecBox::new("npm install")
        .with_state(BoxState::Running {
            start: Instant::now() - Duration::from_secs(10),
            spinner_frame: 0,
        });
    // Should show progress bar
}

#[test]
fn test_exec_progress_updates_over_time() {
    let mut exec = ExecBox::new("sleep 60")
        .with_state(BoxState::running());
    exec.tick();
    // Progress should advance
}

#[test]
fn test_exec_progress_max_after_timeout() {
    let exec = ExecBox::new("long_task")
        .with_state(BoxState::Running {
            start: Instant::now() - Duration::from_secs(120),
            spinner_frame: 0,
        });
    // Progress bar should be full (clamped at 100%)
}
```

---

## Phase 8: FetchBox HTTP Method Badges

**File:** `src/tui/widgets/task_box/fetch.rs`

### Changes

1. **Add render_method_badge():**
```rust
fn render_method_badge(&self) -> Span<'static> {
    let (color, modifier) = match self.method.to_uppercase().as_str() {
        "GET" => (Color::Cyan, Modifier::empty()),
        "POST" => (Color::Green, Modifier::BOLD),
        "PUT" => (Color::Yellow, Modifier::BOLD),
        "DELETE" => (Color::Red, Modifier::BOLD),
        "PATCH" => (Color::Magenta, Modifier::empty()),
        _ => (Color::Gray, Modifier::empty()),
    };

    Span::styled(
        format!("[{}]", self.method.to_uppercase()),
        Style::default().fg(color).add_modifier(modifier)
    )
}
```

2. **Integrate into render:**
```rust
// Header: 🛰️ [GET] https://api.example.com 200
let line = Line::from(vec![
    Span::styled("🛰️ ", Style::default().fg(VerbColor::Fetch.rgb())),
    self.render_method_badge(),
    Span::raw(" "),
    Span::styled(&self.url, Style::default().fg(Color::White)),
    self.render_status_badge(),
]);
```

### Tests

```rust
#[test]
fn test_fetch_get_method_shows_cyan_badge() {
    let fetch = FetchBox::new("GET", "https://api.example.com");
    // GET badge should be cyan
}

#[test]
fn test_fetch_post_method_shows_green_badge() {
    let fetch = FetchBox::new("POST", "https://api.example.com");
    // POST badge should be green + bold
}

#[test]
fn test_fetch_delete_method_shows_red_badge() {
    let fetch = FetchBox::new("DELETE", "https://api.example.com");
    // DELETE badge should be red + bold
}

#[test]
fn test_fetch_unknown_method_shows_gray_badge() {
    let fetch = FetchBox::new("PATCH", "https://api.example.com");
    // PATCH badge should be magenta
}
```

---

## Phase 9: FetchBox Response Preview

**File:** `src/tui/widgets/task_box/fetch.rs`

### Changes

1. **Add render_response_preview():**
```rust
fn render_response_preview(&self, body: &str, max_lines: usize) -> Vec<Line<'static>> {
    let mut lines = Vec::new();

    // Try to parse as JSON for pretty printing
    if let Ok(json) = serde_json::from_str::<serde_json::Value>(body) {
        let pretty = serde_json::to_string_pretty(&json)
            .unwrap_or_else(|_| body.to_string());

        for line in pretty.lines().take(max_lines) {
            lines.push(Line::from(
                Span::styled(format!("  {}", line),
                    Style::default().fg(Color::Gray))));
        }

        if pretty.lines().count() > max_lines {
            lines.push(Line::from(
                Span::styled(format!("  ... ({} more lines)",
                    pretty.lines().count() - max_lines),
                    Style::default().fg(Color::DarkGray))));
        }
    } else {
        // Plain text
        for line in body.lines().take(max_lines) {
            lines.push(Line::from(
                Span::styled(format!("  {}", line),
                    Style::default().fg(Color::Gray))));
        }
    }

    lines
}
```

### Tests

```rust
#[test]
fn test_fetch_expanded_shows_truncated_response() {
    let response = r#"{"users": [{"id": 1}, {"id": 2}]}"#;
    let fetch = FetchBox::new("GET", "https://api.example.com")
        .with_response_body(response);
    // Expanded mode should show max 3 lines
}

#[test]
fn test_fetch_full_shows_entire_response() {
    let response = "Line 1\nLine 2\nLine 3\nLine 4\nLine 5";
    let fetch = FetchBox::new("GET", "https://api.example.com")
        .with_response_body(response);
    // Full mode should show all lines
}

#[test]
fn test_fetch_response_pretty_prints_json() {
    let json = r#"{"status":"ok","count":42}"#;
    let fetch = FetchBox::new("GET", "https://api.example.com")
        .with_response_body(json);
    // JSON should be formatted with indentation
}

#[test]
fn test_fetch_response_shows_content_type() {
    let fetch = FetchBox::new("GET", "https://api.example.com")
        .with_content_type("application/json")
        .with_response_body(r#"{"status":"ok"}"#);
    // Content-Type header should be visible
}
```

---

## Phase 10: Tool Icon Mapping System

**File:** `src/tui/icons.rs` (new module: `mcp_tool`)

### Changes

```rust
pub mod mcp_tool {
    // NovaNet tools
    pub const NOVANET_DESCRIBE: &str = "🧠";
    pub const NOVANET_SEARCH: &str = "🔍";
    pub const NOVANET_TRAVERSE: &str = "🔗";
    pub const NOVANET_ASSEMBLE: &str = "📦";
    pub const NOVANET_ATOMS: &str = "⚛️";
    pub const NOVANET_GENERATE: &str = "✨";
    pub const NOVANET_QUERY: &str = "🔎";
    pub const NOVANET_INTROSPECT: &str = "🔬";

    // Filesystem tools
    pub const FS_READ: &str = "📄";
    pub const FS_WRITE: &str = "✏️";
    pub const FS_LIST: &str = "📁";
    pub const FS_DELETE: &str = "🗑";

    pub const GENERIC_TOOL: &str = "🔧";

    pub fn icon(tool: &str) -> &'static str {
        let lower = tool.to_lowercase();
        if lower.contains("novanet_describe") { NOVANET_DESCRIBE }
        else if lower.contains("novanet_search") { NOVANET_SEARCH }
        else if lower.contains("novanet_traverse") { NOVANET_TRAVERSE }
        else if lower.contains("novanet_assemble") { NOVANET_ASSEMBLE }
        else if lower.contains("novanet_atoms") { NOVANET_ATOMS }
        else if lower.contains("novanet_generate") { NOVANET_GENERATE }
        else if lower.contains("novanet_query") { NOVANET_QUERY }
        else if lower.contains("novanet_introspect") { NOVANET_INTROSPECT }
        else if lower.contains("fs_read") || lower.contains("read_file") { FS_READ }
        else if lower.contains("fs_write") || lower.contains("write_file") { FS_WRITE }
        else if lower.contains("fs_list") || lower.contains("list_dir") { FS_LIST }
        else if lower.contains("fs_delete") { FS_DELETE }
        else { GENERIC_TOOL }
    }
}
```

### Tests

```rust
#[test]
fn test_novanet_describe_icon() {
    assert_eq!(mcp_tool::icon("novanet_describe"), "🧠");
}

#[test]
fn test_fs_read_icon() {
    assert_eq!(mcp_tool::icon("fs_read"), "📄");
}

#[test]
fn test_generic_tool_fallback() {
    assert_eq!(mcp_tool::icon("unknown_tool"), "🔧");
}

#[test]
fn test_all_novanet_tools_have_distinct_icons() {
    let icons = [
        mcp_tool::icon("novanet_describe"),
        mcp_tool::icon("novanet_search"),
        mcp_tool::icon("novanet_traverse"),
        mcp_tool::icon("novanet_generate"),
    ];
    // All should be unique
}
```

---

## Phase 13: TaskStarted Event Handler

**File:** `src/tui/views/chat.rs`

### Changes

1. **Add task_boxes HashMap:**
```rust
pub struct ChatView {
    // ... existing fields ...
    task_boxes: HashMap<Arc<str>, TaskBox>,
    task_states: HashMap<Arc<str>, BoxState>,
}
```

2. **Implement TaskStarted handler:**
```rust
EventKind::TaskStarted { task_id, action } => {
    let task_box = TaskBox::new(
        task_id.to_string(),
        action.verb_name(),
        BoxState::Pending,
    );
    self.task_boxes.insert(task_id.clone(), task_box);
    self.task_states.insert(task_id.clone(), BoxState::Pending);
}
```

3. **Update TaskCompleted handler:**
```rust
EventKind::TaskCompleted { task_id, result } => {
    if let Some(task_box) = self.task_boxes.get_mut(&task_id) {
        task_box.set_state(BoxState::Success {
            duration: result.duration
        });
    }
}
```

### Tests

```rust
#[test]
fn test_task_started_creates_task_box() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted {
        task_id: Arc::from("task-1"),
        action: TaskAction::Infer { prompt: "test".into(), model: None },
    });
    assert!(view.task_boxes.contains_key(&Arc::from("task-1")));
}

#[test]
fn test_task_completed_updates_state() {
    let mut view = ChatView::new();
    // Start task
    view.handle_event(EventKind::TaskStarted { ... });
    // Complete task
    view.handle_event(EventKind::TaskCompleted {
        task_id: Arc::from("task-1"),
        result: TaskResult { duration: Duration::from_secs(2), ... },
    });
    // Verify state is Success
}

#[test]
fn test_task_failed_shows_error() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted { ... });
    view.handle_event(EventKind::TaskFailed {
        task_id: Arc::from("task-1"),
        error: Arc::from("Command not found"),
    });
    // Verify state is Failed with error message
}
```

---

## Phase 14: Theme-Aware BoxState

**File:** `src/tui/widgets/task_box/state.rs`

### Changes

1. **Add color_from_theme() method:**
```rust
impl BoxState {
    pub fn color_from_theme(&self, theme: &Theme) -> Color {
        match self {
            BoxState::Queued => theme.status_pending,
            BoxState::Running { .. } => theme.status_running,
            BoxState::Success { .. } => theme.status_success,
            BoxState::Failed { .. } => theme.status_failed,
            BoxState::Skipped { .. } => theme.text_muted,
        }
    }
}
```

2. **Update TaskBox::widget() to use theme:**
```rust
pub fn widget<'a>(&'a self, theme: &Theme) -> impl Widget + 'a {
    let color = self.state.color_from_theme(theme);
    // ... use color for border/text
}
```

### Tests

```rust
#[test]
fn test_box_state_uses_theme_colors() {
    let theme = Theme::solarized_light();
    let state = BoxState::Success { duration: Duration::from_secs(1) };
    assert_eq!(state.color_from_theme(&theme), theme.status_success);
}

#[test]
fn test_theme_switch_changes_colors() {
    let light = Theme::solarized_light();
    let dark = Theme::dark();
    let state = BoxState::Running { start: Instant::now(), spinner_frame: 0 };
    assert_ne!(
        state.color_from_theme(&light),
        state.color_from_theme(&dark)
    );
}
```

---

## Phase 15: Animation Synchronization

**File:** `src/tui/views/chat.rs`

### Changes

1. **Add AnimationTicker to ChatView:**
```rust
pub struct ChatView {
    animation_ticker: AnimationTicker,
    current_frame: u16,
}
```

2. **Implement tick_animation():**
```rust
pub fn tick_animation(&mut self) {
    self.current_frame = self.animation_ticker.tick();

    for task_box in self.task_boxes.values_mut() {
        if let BoxState::Running { ref mut spinner_frame, .. } = task_box.state_mut() {
            *spinner_frame = (self.current_frame % 8) as u8;
        }
    }
}
```

3. **Wire to event loop (60 FPS):**
```rust
tokio::select! {
    _ = tokio::time::sleep(Duration::from_millis(16)) => {
        chat_view.tick_animation();
    }
}
```

### Tests

```rust
#[test]
fn test_pulse_cycle_8_frames() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted { ... });

    let mut phases = Vec::new();
    for _ in 0..8 {
        view.tick_animation();
        // Collect phases
    }
    assert_eq!(phases, vec![0, 1, 2, 3, 4, 5, 6, 7]);
}

#[test]
fn test_animation_stops_on_completion() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted { ... });
    view.tick_animation();
    view.handle_event(EventKind::TaskCompleted { ... });
    view.tick_animation();
    // State should remain Completed, not Running
}
```

---

## Phase 16: TokenVelocity Wiring

**File:** `src/tui/widgets/task_box/agent.rs`

### Changes

1. **Add process_agent_turn() method:**
```rust
impl AgentBox {
    pub fn process_agent_turn(
        &mut self,
        metadata: &AgentTurnMetadata,
        turn_duration_ms: u64,
    ) {
        self.tokens_in += metadata.input_tokens;
        self.tokens_out += metadata.output_tokens;

        if turn_duration_ms > 0 {
            let velocity = metadata.output_tokens as f32
                / (turn_duration_ms as f32 / 1000.0).max(0.001);
            self.push_velocity_sample(velocity);
        }

        self.turn += 1;
    }
}
```

2. **Update AgentTurn handler in ChatView:**
```rust
EventKind::AgentTurn { task_id, metadata, .. } => {
    if let Some(agent_box) = self.get_agent_box_mut(&task_id) {
        if let Some(meta) = metadata {
            let elapsed = /* calculate from previous turn */;
            agent_box.process_agent_turn(&meta, elapsed);
        }
    }
}
```

### Tests

```rust
#[test]
fn test_velocity_calculated_from_samples() {
    let mut agent = AgentBox::new("task-1", "prompt");
    agent.process_agent_turn(&AgentTurnMetadata {
        input_tokens: 100,
        output_tokens: 200,
        thinking: None,
    }, 1000); // 1 second

    assert!((agent.velocity.average() - 200.0).abs() < 10.0);
}

#[test]
fn test_agent_turn_updates_velocity() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted { ... });
    view.handle_event(EventKind::AgentTurn {
        task_id: Arc::from("task-1"),
        metadata: Some(AgentTurnMetadata {
            input_tokens: 100,
            output_tokens: 150,
            thinking: None,
        }),
        ..
    });
    // Verify velocity tracker has samples
}
```

---

## Phase 17: AgentSpawned Visualization

**File:** `src/tui/views/chat.rs` and `src/tui/widgets/task_box/agent.rs`

### Changes

1. **Add agent_hierarchy to ChatView:**
```rust
pub struct ChatView {
    agent_hierarchy: HashMap<Arc<str>, Vec<Arc<str>>>,
}
```

2. **Implement AgentSpawned handler:**
```rust
EventKind::AgentSpawned { parent_task_id, child_task_id, depth, prompt } => {
    self.agent_hierarchy
        .entry(parent_task_id.clone())
        .or_insert_with(Vec::new)
        .push(child_task_id.clone());

    let child = AgentBox::new(child_task_id.to_string(), prompt, depth);
    self.active_agent_boxes.insert(child_task_id, child);
}
```

3. **Render nested children:**
```rust
fn render_agent_with_children(&self, parent: &AgentBox, children: &[&AgentBox]) -> impl Widget {
    // Render parent
    // For each child, render indented with tree connector
    // ├── child-1
    // └── child-2
}
```

### Tests

```rust
#[test]
fn test_agent_spawned_creates_hierarchy() {
    let mut view = ChatView::new();
    view.handle_event(EventKind::TaskStarted { task_id: "parent" });
    view.handle_event(EventKind::AgentSpawned {
        parent_task_id: Arc::from("parent"),
        child_task_id: Arc::from("child"),
        depth: 1,
        prompt: "subtask".into(),
    });

    assert!(view.agent_hierarchy.get(&Arc::from("parent"))
        .map(|v| v.contains(&Arc::from("child")))
        .unwrap_or(false));
}

#[test]
fn test_multiple_children_rendered() {
    let mut view = ChatView::new();
    // Spawn 3 children
    // Verify all are in hierarchy
}

#[test]
fn test_depth_limit_visualization() {
    // Create 3-level hierarchy
    // Verify depth is tracked correctly
}
```

---

## Code Quality Improvements

### ExecBox Performance Fixes

**File:** `src/tui/widgets/task_box/exec.rs`

1. **Add caching for static strings:**
```rust
pub struct ExecBox {
    // Cached strings (computed once, reused per render)
    cached_title: Option<String>,
    cached_separator: Option<String>,
    state_changed: bool,
}
```

2. **Fix cwd double allocation:**
```rust
// Before: 4 allocations
let cwd_str = self.cwd.as_ref().map(|c| {
    let display = if c.chars().count() > 20 {
        format!("...{}", c.chars().skip(c.chars().count() - 17).collect::<String>())
    } else {
        c.clone()  // ❌ Unnecessary clone
    };
    format!(" │ cwd: {}", display)
});

// After: 1-2 allocations
let cwd_str = self.cwd.as_ref().map(|c| {
    if c.chars().count() > 20 {
        format!(" │ cwd: ...{}", c.chars().skip(c.chars().count() - 17).collect::<String>())
    } else {
        format!(" │ cwd: {}", c)  // No clone
    }
});
```

3. **Convert exit_display() to Cow:**
```rust
fn exit_display(&self) -> Cow<'static, str> {
    match self.exit_code {
        Some(0) => Cow::Borrowed("exit: 0 ✓"),
        Some(code) => Cow::Owned(format!("exit: {} ✗", code)),
        None => Cow::Borrowed("exit: ?"),
    }
}
```

### ExecBox Missing Tests

```rust
#[test]
fn test_truncate_empty() {
    assert_eq!(ExecBox::truncate("", 10), "");
}

#[test]
fn test_truncate_exact_length() {
    assert_eq!(ExecBox::truncate("12345", 5), "12345");
}

#[test]
fn test_truncate_max_len_3() {
    assert_eq!(ExecBox::truncate("hello", 3), "hel");
}

#[test]
fn test_truncate_utf8() {
    assert_eq!(ExecBox::truncate("Hello 世界", 8), "Hello...");
}

#[test]
fn test_truncate_one_over() {
    assert_eq!(ExecBox::truncate("hello", 4), "h...");
}

#[test]
fn test_render_zero_area() {
    let box_ = ExecBox::new("cmd");
    let mut buf = Buffer::empty(Rect::new(0, 0, 0, 0));
    box_.render(Rect::new(0, 0, 0, 0), &mut buf);
    // Should not panic
}

#[test]
fn test_render_minimal_area() {
    let box_ = ExecBox::new("cmd");
    let mut buf = Buffer::empty(Rect::new(0, 0, 30, 4));
    box_.render(Rect::new(0, 0, 30, 4), &mut buf);
    // Should render
}

#[test]
fn test_render_long_command() {
    let box_ = ExecBox::new("x".repeat(1000));
    let mut buf = Buffer::empty(Rect::new(0, 0, 50, 10));
    box_.render(Rect::new(0, 0, 50, 10), &mut buf);
    // Should truncate, not panic
}

#[test]
fn test_render_many_stdout_lines() {
    let many_lines = (0..100).map(|i| format!("line {}", i)).join("\n");
    let mut box_ = ExecBox::new("cmd").with_stdout(many_lines);
    box_.expanded_stdout = true;
    let mut buf = Buffer::empty(Rect::new(0, 0, 80, 20));
    box_.render(Rect::new(0, 0, 80, 20), &mut buf);
    // Should limit to 10 lines
}

#[test]
fn test_exit_display_borrows_common_cases() {
    let success = ExecBox::new("cmd").with_exit_code(0);
    assert!(matches!(success.exit_display(), Cow::Borrowed(_)));
}
```

---

## Build Sequence

### Week 1: InferBox + ExecBox

| Day | Tasks | Tests |
|-----|-------|-------|
| 1 | Phase 1 (Thinking) | 5 tests |
| 2 | Phase 2 (Badges), Phase 3 (Velocity) | 8 tests |
| 3 | Phase 4 (Progress) | 6 tests |
| 4 | Phase 6 (Compact), Phase 7 (Progress) | 8 tests |
| 5 | Code Quality (truncate, render safety) | 12 tests |

### Week 2: FetchBox + InvokeBox + Wiring

| Day | Tasks | Tests |
|-----|-------|-------|
| 1 | Phase 8 (HTTP Badges), Phase 9 (Preview) | 8 tests |
| 2 | Phase 10 (Icons), Phase 11 (Params) | 9 tests |
| 3 | Phase 12 (Retry), Phase 13 (TaskStarted) | 7 tests |
| 4 | Phase 14 (Theme), Phase 15 (Animation) | 4 tests |
| 5 | Phase 16 (Velocity), Phase 17 (Hierarchy) | 5 tests |
| 6 | Integration testing, polish | N/A |

---

## Success Criteria

### Tests
- [ ] 91 new tests passing
- [ ] Total test count: 2989 + 91 = 3080 tests
- [ ] Zero clippy warnings
- [ ] All render paths tested for safety

### Performance
- [ ] ExecBox allocations reduced from 13 to <5 per frame
- [ ] Animation runs at 60 FPS without frame drops
- [ ] No blocking operations in render path

### Visual
- [ ] Thinking section renders with 💭 icon
- [ ] Model badges show text (not just icons)
- [ ] HTTP method badges are color-coded
- [ ] Child agents render nested inside parent
- [ ] Progress bars update smoothly

---

## File Summary

| File | Lines Added | Lines Modified |
|------|-------------|----------------|
| `src/tui/widgets/task_box/infer.rs` | ~150 | ~50 |
| `src/tui/widgets/task_box/exec.rs` | ~100 | ~80 |
| `src/tui/widgets/task_box/fetch.rs` | ~120 | ~40 |
| `src/tui/widgets/task_box/invoke.rs` | ~80 | ~30 |
| `src/tui/widgets/task_box/agent.rs` | ~60 | ~20 |
| `src/tui/widgets/task_box/state.rs` | ~30 | ~10 |
| `src/tui/views/chat.rs` | ~200 | ~50 |
| `src/tui/icons.rs` | ~60 | ~5 |
| **Total** | **~800** | **~285** |

---

## References

- Theme System: `src/tui/theme.rs` (1790 lines)
- Animation System: `src/tui/widgets/animation.rs`
- BoxState: `src/tui/widgets/task_box/state.rs`
- VerbColor: `src/tui/widgets/task_box/colors.rs`
- PERFORMANCE.md: `.claude/rules/PERFORMANCE.md`
