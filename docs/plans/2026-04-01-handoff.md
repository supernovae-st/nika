# HANDOFF — Session du 1er Avril 2026

> Donne ce fichier a un agent Claude Code pour reprendre le travail.
> Working directory: `/Users/thibaut/dev/supernovae/nika`

---

## Ce qui a ete fait aujourd'hui

### 1. test-jungo — Dashboard interactif pour nika serve

**Path:** `/Users/thibaut/dev/supernovae/test-jungo`
**Git status:** TOUT non-commit (repo separe, pas un submodule de nika)

Site Nuxt 3 + @nuxt/ui (teal+slate dark theme) qui sert de test client ET documentation interactive pour l'API nika serve.

**4 pages :**
- `/` — API Playground : build request, HTTP preview, run workflow, sequence diagram anime (3 lifelines Client↔Server↔LLM), "What just happened?" timeline annotee, Network Inspector (request/response side-by-side), Output tab, History tab avec re-run
- `/docs` — Nika language reference : 5 verbs avec exemples YAML, data flow (bindings, transforms), structured output (5 couches), 9 providers, resources
- `/api` — API reference avec TryItBlock interactifs + guided tour 4 etapes (health → submit → poll → result). Endpoints, config, rate limiting, SSE, webhooks, metrics, errors, internals
- `/workflows` — Browse 44 workflows, DAG visualization (SVG, verb colors, layers), YAML source, task detail popup, one-click run

**4 composants :**
- `SequenceDiagram.vue` — 3 lifelines animees Client↔Server↔LLM avec replay, mobile responsive
- `WorkflowGraph.vue` — DAG SVG avec verb colors (infer=violet, exec=amber, fetch=sky, invoke=emerald, agent=rose), topological layers, cycle detection, block-list depends_on parsing
- `TryItBlock.vue` — blocs interactifs (editable body, Try It button, Copy cURL, response with status/timing)
- `CopyButton.vue` — clipboard copy avec try/catch pour HTTP/Safari

**Layout :** Sidebar avec nav, connection status (pastille verte/rouge), exponential backoff health check (5s connected, 2s→15s disconnected), disconnect banner anime, reconnect toast

**Composable `useNika.ts` :** HTTP exchange tracking (tracedFetch), job state management, exchange counter via useState (HMR-safe), path display normalization (regex /api → /v1)

**44 workflows** dans `workflows/` couvrant tous les verbs et features Nika.

**Note technique :** Le cache `.nuxt` se corrompt regulierement. Fix : `rm -rf .nuxt && npm run dev`

### 2. Bug report + master plan nika serve

**Path:** `/Users/thibaut/dev/supernovae/nika/docs/plans/2026-04-01-serve-bug-report.md`
**Taille :** ~1100 lignes

Contient :
- **11 bugs** (4 CRITICAL, 7 IMPORTANT) avec fichier:ligne exact et code fix
- **3 gaps architecture** (single output, artifacts filesystem, zero progress)
- **3 innovations** a implementer (Dagger LLM cache, Restate checkpoints, DBOS --resume)
- **Roadmap V3** complete : API enrichie, typed SSE, artifact download, SDK natif
- **SDK architecture** : Rust core + napi-rs (Node.js) + PyO3 (Python) + WASM (browser)
- **Performance architecture** : mmap zero-copy, BLAKE3, SQLite WAL 8 readers, SSE ~100us
- **Output pipeline trace** complet : LLM → engine → storage → API (avec code paths)
- **Comparison table** vs Temporal, Windmill, Hatchet, LangChain

### 3. Recherche (8 rapports)

**Path:** `/Users/thibaut/dev/supernovae/test-jungo/docs/research/`

| Rapport | Contenu |
|---------|---------|
| `workflow-output-api-patterns.md` | 7 engines : Temporal, Windmill, Hatchet, Prefect, Airflow, Inngest, GitHub Actions |
| `async-job-streaming-patterns.md` | 6 AI APIs : OpenAI, Replicate, Modal, Vercel AI, Cloudflare Workers |
| `artifact-serving-patterns.md` | 5 CI/CD : GitHub, GitLab, Buildkite, S3 presigned URLs, CAS |
| `workflow-sdk-dx-patterns.md` | 6 SDKs : Hatchet, Trigger.dev, Windmill, Temporal, Inngest, BullMQ |
| `workflow-engine-architecture-research.md` | 5 architectures : Temporal, Windmill, Restate, DBOS, Dagger |
| `nika-sdk-architecture.md` | SDK : napi-rs, PyO3, WASM, utoipa, embedded vs remote |
| `streaming-performance-and-storage-patterns.md` | Perf : mmap, BLAKE3, SQLite WAL, SSE 100k connections, turso/libsql |
| `workflow-engine-http-api-comparison.md` | API deep dive : Temporal vs Inngest vs Windmill JSON responses |

### 4. Decouverte npm existant

Le CI (`release.yml`) publie deja le binaire CLI via npm :
- `@supernovae/nika` (wrapper `packages/npm/bin.js` → lance le binaire natif)
- `@supernovae/nika-darwin-arm64`, `-darwin-x64`, `-linux-x64`, `-linux-arm64`, `-win32-x64`
- Pattern identique a esbuild/SWC/Turbo
- Version actuelle dans package.json : 0.46.1 (desync avec le binaire 0.58.0)

Le SDK programmatique sera un DEUXIEME package npm : `@supernovae/nika-sdk` (napi-rs, API programmatique, pas un bin wrapper).

### 5. Bugs fixes dans test-jungo (2 rounds d'audit)

**Round 1 (6 agents) :** 13 bugs fixes — isRunning logic, path transform, exchangeCounter HMR, empty catch, await useFetch, parseYamlMeta memo, server validation (body, ID regex), health error forwarding, workflow providers (3 sans provider)

**Round 2 (review) :** 5 bugs fixes — $refs crash dans api.vue tour, CopyButton try/catch, cycle guard WorkflowGraph, viewJob reset state, depends_on block-list parsing

---

## Comment organiser le travail

### Phase 1 — Bugs critiques (v0.58.1) — 1 session, ~2h

**Objectif :** Nicolas voit un output propre et le polling est stable.

**Plan :** `nika/docs/plans/2026-04-01-serve-bug-report.md` → BUG-1, BUG-10, BUG-2, BUG-3

**Prompt pour l'agent :**
> Lis `nika/docs/plans/2026-04-01-serve-bug-report.md`. Execute Wave 1 (BUG-1 + BUG-10) puis Wave 2 premiers bugs (BUG-2 + BUG-3). Chaque fix dans un commit separe. `cargo test --workspace --lib` apres chaque commit.

**Taches detaillees :**
1. **BUG-1** — `tools/nika-serve/src/config.rs:15-16` : switch `#[default]` de Subprocess a Embedded
2. **BUG-1** — `config.rs:96` : changer `.unwrap_or("subprocess")` → `"embedded"`
3. **BUG-1** — Update test fixtures : `executor.rs:~155`, `lib.rs:~329` → `ExecutorMode::Embedded`
4. **TEST** — `cargo test --workspace --lib` doit passer
5. **E2E** — `nika serve` + `curl POST /v1/run` + poll → verifier output propre (pas de Performance/Timeline)
6. **BUG-10** — `test-jungo/pages/index.vue` : remplacer `setInterval` par `setTimeout` chain + `pollAborted` guard (code exact dans le bug report)
7. **BUG-2** — `tools/nika-serve/src/worker.rs:288-297` : remplacer `k.ends_with("_KEY")` par explicit allowlist, supprimer `NIKA_VAULT_PASSPHRASE`
8. **BUG-3** — `tools/nika-serve/src/lib.rs:196-207` : deplacer `shutdown_tx.send(true)` dans la closure du signal, avant que Axum stop
9. **TAG** — `git tag v0.58.1`

### Phase 2 — Bugs restants (v0.58.2) — 1 session, ~3h

**Plan :** BUG-4 to BUG-11

| Bug | Fichier | Fix |
|-----|---------|-----|
| BUG-4 | `routes/workflows.rs:92` | Decrementer active_jobs si create_job echoue |
| BUG-5 | `events.rs:86` | Verifier job existe avant subscribe SSE |
| BUG-6 | `worker.rs:275` + `routes/workflows.rs` | Regex validation input keys + max 64 keys |
| BUG-7 | `routes/mod.rs:23` | Ajouter require_auth au metrics router |
| BUG-8 | `worker.rs:144` | Charger WebhookConfig au startup, pas par job |
| BUG-9 | `executor.rs:65` | Ajouter warn! au startup si embedded mode |
| BUG-11 | N/A | Documente seulement (resolu par BUG-1 embedded) |

### Phase 3 — Rich API (v0.59) — 2-3 sessions

1. Enrichir StatusResponse → `tasks` + `progress` + `cost` + `artifacts` manifest
2. Embedded executor : capturer per-task results depuis le Datastore avant drop
3. `output:` workflow-level declaration dans le YAML (explicit single, multi, artifacts)
4. Rename `/v1/status/{id}` → `/v1/jobs/{id}` (garder ancien comme alias)
5. Content-addressable LLM cache : BLAKE3 hash(provider+model+temp+prompt+schema), SQLite store

### Phase 4 — Artifacts + Streaming (v0.60) — 2-3 sessions

1. `GET /v1/jobs/{id}/artifacts` — listing (manifest depuis SQLite)
2. `GET /v1/jobs/{id}/artifacts/{path}` — download (mmap LRU cache, ETag = CAS hash, zero-copy Axum body)
3. Typed SSE events : `task_started`, `task_completed`, `progress`, `artifact_written`, `workflow_completed`
4. Checkpoint store : SQLite table `checkpoints(run_id, task_id, output, completed_at)`
5. `nika run --resume <run_id>` + `nika run --from <task_id>` flags
6. Storage : `artifact_manifest TEXT` column + `append_output()` method

### Phase 5 — SDK (v0.61) — 3-4 sessions

1. `nika-sdk-core` Rust crate : embedded mode (in-process Runner) + remote mode (HTTP client)
2. `@supernovae/nika-sdk` via napi-rs : `run()`, `stream()`, `artifact()`, auto-retry, typed results
3. `nika-py` via PyO3 : meme API que Node.js
4. CI : ajouter platform packages `@supernovae/nika-sdk-{platform}` dans `packages/`

### Phase 6 — Universal (v0.62) — 1-2 sessions

1. OpenAPI spec via utoipa sur les handlers Axum
2. `@nika/wasm` via wasm-pack (browser/Deno/Bun)
3. Generated clients Go/Java/Ruby via openapi-generator

---

## Fichiers importants a lire (par ordre de priorite)

| Fichier | Pourquoi |
|---------|----------|
| **`nika/docs/plans/2026-04-01-serve-bug-report.md`** | LE master plan — bugs, fixes, roadmap, architecture, tout |
| **`nika/CLAUDE.md`** | Language reference + project structure (.git principle, nika.toml) |
| **`~/.claude/rules/nika-bugs-and-patterns.md`** | Bugs connus du workflow engine (BUG-001 to BUG-013) |
| `nika/tools/nika-serve/src/config.rs` | BUG-1 : ExecutorMode default |
| `nika/tools/nika-serve/src/worker.rs` | BUG-2 (env), BUG-6 (inputs), BUG-8 (webhook) |
| `nika/tools/nika-serve/src/lib.rs` | BUG-3 (shutdown), server bootstrap, tests |
| `nika/tools/nika-serve/src/routes/workflows.rs` | BUG-4 (counter), status response structure |
| `nika/tools/nika-serve/src/events.rs` | BUG-5 (SSE orphans) |
| `nika/tools/nika-serve/src/executor.rs` | Embedded executor, BUG-9 (panic) |
| `nika/tools/nika-storage/src/lib.rs` | SQLite schema, job operations, actor pattern |
| `nika/tools/nika-engine/src/runtime/runner.rs` | get_final_output():539, run():1421, render_summary() |
| `nika/packages/npm/` | Existing npm distribution (bin.js wrapper, 5 platform packages) |
| `test-jungo/` | Dashboard Nuxt 3 (CLAUDE.md dedans pour le contexte) |
| `test-jungo/docs/research/` | 8 rapports de recherche competitive |

## Regles imperatives

- `cargo test --workspace --lib` (TOUJOURS --lib, evite les keychain popups macOS)
- Commits: `type(scope): description` + co-authors (Claude + Nika)
- AGPL-3.0-or-later pour tous les crates Nika
- Zero backward compat (v0.x, zero users en prod)
- Test BEFORE commit. Edge cases always.
- 1 fix = 1 commit. Pas de batch.
- Prompts dans les tests = NATURELS (jamais mentionner JSON quand structured: est utilise)

## Etat des VPS

| VPS | IP | Nika Version | Status |
|-----|----|-------------|--------|
| nk-dev-vps | 51.15.136.200 | v0.51 (outdated!) | Daemon running, systemd Restart=always |
| nk-jungo-vps | (pas cree) | - | A creer sur Scaleway PLAY2-NANO fr-par-1 |
| nk-h100-gpu | 51.159.153.241 | - | vLLM UP (Qwen3.5-27B :8000 + Qwen3-0.6B :8001) |

**Apres v0.58.1 :** deployer le binaire sur nk-dev-vps (scp + restart daemon).

## Etat git

**nika repo :** Plusieurs fichiers modifies + le handoff/bug-report non-commites. Commiter les plans avant de commencer les fixes.

**test-jungo repo :** 100% des fichiers Nuxt non-commites. C'est un repo git separe (`/Users/thibaut/dev/supernovae/test-jungo/.git`), pas un submodule de nika. A commiter separement.

## npm version desync

`packages/npm/package.json` a version `0.46.1` alors que le binaire est `0.58.0-dev`. Le CI met a jour automatiquement a la release, mais c'est un signal que la derniere release npm date de v0.46.

## Contexte Nicolas

Nicolas developpe QR Code AI (qrcode-ai.com) en Node.js/TypeScript. Il a besoin de traduire 17 fichiers JSON d'UI en 200 locales. Il appelle `nika serve` depuis son backend via axios. Il ne connait ni Rust, ni vLLM, ni les daemons. Il fait POST, poll GET, recupere le resultat.

Le projet `nk-jungo` (`/Users/thibaut/dev/supernovae/nk-jungo/`) contient les vrais workflows de traduction + les 200 dossiers de contexte local. Le `HANDOFF.md` de nk-jungo a le spec complet des 4 workflows a ecrire.
