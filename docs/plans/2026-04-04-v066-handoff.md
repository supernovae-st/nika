# v0.66.0 Handoff — Security + Stabilization Sprint

> Date: 2026-04-04
> Version: v0.66.0 (tagged, pushed)
> Tests: 9,819 passing | 0 clippy warnings
> Commits: 10 (4 security + 2 refactor + 3 docs + 1 release)

## What was done

### Phase 1: Security (4 fixes)
1. **SEC-1**: Removed 4KB scan limit from exec blocklist. All 3 functions (check_blocklist, check_shell_mode_blocklist, check_blocklist_with_intent) now scan full command.
2. **SEC-2**: Upgraded shell:true injection from warning to NIKA-053 error. 30 test bindings updated to use `| shell`. Serve-api showcase updated.
3. **SEC-3**: Applied redact_secrets() to all BlockedCommand error variants. API keys no longer leak in error messages.
4. **SEC-4**: Added file size pre-check to nika:read (50MB limit via metadata).

### Phase 2: Architecture (2 changes)
5. **Split data_tools.rs** (2122 lines → 6 files under builtin/data/): merge.rs (390), transform.rs (605), aggregate.rs (392), jq.rs (313), text.rs (310), io.rs (203). Zero behavior changes.
6. **Deprecated nika:json_query** — added tracing::warn on each call, pointing to nika:jq.

### Phase 3: DX (3 updates)
7. Updated claude.md (source of truth for ~/.claude/rules/nika.md): 50 transforms, 61 tools, security notes, error codes.
8. Updated nika-bugs-and-patterns.md: new tool docs, SEC-2 enforcement, security hardening section.
9. Updated AGENTS.md: metrics refresh.

### Phase 4: Verification
10. All 8 showcase workflows pass nika check.
11. Serve-api showcase fixed for SEC-2 compliance.

### Phase 5: Release
12. Version bumped to 0.66.0, CHANGELOG updated, tagged, pushed.

## What was NOT done (deferred)
- jaq 3.x upgrade → v0.67 (8 days old, API rewrite)
- vault.rs in nika-core → v0.67 (violates zero I/O)
- nika-engine depends on nika-init → v0.67 (inverted dependency)
- Homebrew formula update (needs release binary)
- Mintlify docs site audit
- Benchmark suite (criterion)
- E2E from fresh directory (needs release binary)

## Metrics
- 50 pipe transforms (19 were undocumented)
- 61 builtin tools (7 core + 5 file + 6 introspection + 13 data + 6 data sprint 2 + 24 media)
- 9,819 tests across 15 crates
- 0 clippy warnings
