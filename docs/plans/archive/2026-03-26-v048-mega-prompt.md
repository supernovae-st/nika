# v0.48 Mega Prompt — Nika Native Daemon

> Give this ENTIRE file to a new Claude Code session.

---

## PROMPT

Lis ces fichiers dans cet ordre exact:
1. `docs/plans/2026-03-26-v048-mega-prompt.md` (CE FICHIER — le master plan)
2. `docs/plans/2026-03-26-nika-daemon-architecture.md` (design daemon complet)
3. `docs/plans/2026-03-26-v047-discovery-triage.md` (29 findings des 9 agents)
4. `tools/nika/CLAUDE.md` + `tools/nika-engine/CLAUDE.md` (conventions codebase)

---

## CONTEXTE

**Nika** = workflow engine YAML pour AI. Schema `nika/workflow@0.12`. Single Rust binary.

**Workspace** (11 crates dans `tools/`):
```
nika           — Binary CLI (2k LOC)
nika-engine    — Execution engine (140k LOC)
nika-init      — Project scaffolding (21k LOC) [NEW: extracted v0.47]
nika-core      — AST + types (23k) — ZERO I/O
nika-event     — EventLog (4k)
nika-mcp       — MCP client (9k)
nika-media     — CAS store + media tools (25k) [EXPANDED: v0.47]
nika-cli       — CLI subcommands (8k)
nika-tui       — Terminal UI (86k) — ratatui
nika-lsp-core  — LSP intelligence (9k)
nika-lsp       — LSP binary (2.5k)
```

**Session précédente (v0.47)** — 22 commits:
- ARCH-1: nika-init extracted, ARCH-2: media → nika-media
- 9 discovery agents: 4 CRITICAL + 10 HIGH + 15 MEDIUM findings
- Legacy `spn-client` daemon REMOVED. `nika-daemon` feature flag GONE.
- C1/C2/C4/H1/H5/H6/H7/H8/M13 FIXED.
- Tests: 8,023 passing, 0 clippy warnings

**Ce qui reste à faire:**
1. **DAEMON NATIF** — le gros chantier (nouveau crate `nika-daemon`)
2. **Discovery remaining** — H2, H3+H4, H9, H10, C3, M1-M15
3. **ARCH-3** — Split NikaError (90 variants → domain enums)

---

## INSTRUCTIONS DÉTAILLÉES

### Workflow obligatoire pour CHAQUE phase

```
1. /spn-powers:brainstorming    → Refine l'approche (sauf pour les 1-line fixes)
2. /spn-rust:rust                → Load Rust patterns AVANT tout code
3. /find-docs                    → Lookup des crates (rusqlite, notify, nix, cron) via Context7
4. TDD (/spn-powers:test-driven-development):
   a. Écrire les tests D'ABORD
   b. Vérifier qu'ils FAIL (RED)
   c. Implémenter le code minimum pour PASS (GREEN)
   d. Refactor si nécessaire
5. /spn-powers:verification-before-completion → cargo test + clippy AVANT tout commit
6. /spn-powers:requesting-code-review → Lancer un agent de review après chaque phase majeure
7. 1 fix = 1 commit granulaire, push tous les 3-5 commits
```

### Phase 0: Orientation (10 min)

```bash
# Vérifier l'état du workspace
cd /Users/thibaut/dev/supernovae/nika/tools
cargo test --workspace --lib 2>&1 | grep "test result:"   # Doit: 8000+ pass, 0 fail
git log --oneline -5                                       # Vérifier HEAD
git status                                                 # Doit: clean (sauf docs/)
```

Lis les 3 docs de plan. Lis CLAUDE.md. Confirme que tu comprends l'architecture.

### Phase 1: nika-daemon Foundation (PRIORITE 1)

**Objectif**: Créer le crate `nika-daemon` avec server, client, protocol. Pouvoir faire `nika daemon start` et `nika daemon status`.

**Étapes détaillées:**

#### 1.1 Créer le crate

```bash
mkdir -p tools/nika-daemon/src/services
mkdir -p tools/nika-daemon/src/lifecycle
```

**Cargo.toml** — utilise `workspace = true` pour version/edition/authors/license. Deps:
- `tokio` (workspace, features = ["net", "signal", "process", "fs", "io-util"])
- `serde` + `serde_json` (workspace)
- `thiserror` (workspace)
- `tracing` (workspace)
- `blake3` (workspace)
- `chrono` (workspace)
- `uuid` (workspace)
- `dirs` (workspace)
- `nix` = "0.29" features = ["signal", "process", "fs", "net"]
- `keyring` = "3" features = ["apple-native", "windows-native", "sync-secret-service"]
- `secrecy` = "0.10"
- Dev: `tempfile`, `tokio` (rt-multi-thread + macros)

**NE PAS** ajouter rusqlite/notify/cron maintenant — Phase 2/3.
**NE PAS** dépendre de nika-engine — le daemon est lightweight.

Ajouter `"nika-daemon"` aux workspace members + workspace.dependencies dans `tools/Cargo.toml`.

#### 1.2 Protocol (TDD)

Fichier: `src/protocol.rs`

**Tests d'abord:**
```rust
#[test] fn request_serialize_ping() { ... }
#[test] fn request_serialize_get_secret() { ... }
#[test] fn response_serialize_pong() { ... }
#[test] fn response_serialize_error() { ... }
#[test] fn roundtrip_request_response() { ... }
#[test] fn wire_format_length_prefix() { ... }
```

**Implémentation:**
- `DaemonRequest` enum (tagged: `#[serde(tag = "type")]`)
- `DaemonResponse` enum (tagged)
- `encode_message(msg: &impl Serialize) -> Vec<u8>` (4-byte BE length + JSON)
- `decode_message<T: DeserializeOwned>(reader: &mut impl AsyncRead) -> Result<T>`
- Voir le design dans `nika-daemon-architecture.md`

#### 1.3 Client (TDD)

Fichier: `src/client.rs`

**Tests d'abord:**
```rust
#[tokio::test] async fn client_connect_to_nonexistent_socket_returns_error() { ... }
#[tokio::test] async fn client_ping_pong_roundtrip() { ... }
#[tokio::test] async fn client_timeout_on_slow_server() { ... }
#[tokio::test] async fn client_is_available_false_when_no_socket() { ... }
```

**Implémentation:**
- `DaemonClient::connect(socket_path) -> Result<Self>`
- `DaemonClient::is_available() -> bool` (checks socket file exists)
- `DaemonClient::send<R: Serialize, T: DeserializeOwned>(&self, request: R) -> Result<T>`
- `DaemonClient::ping() -> Result<PongInfo>`
- Timeout: 5 secondes par défaut
- Connection pooling: pas nécessaire (1 connection par requête, Unix socket = fast)

#### 1.4 Server (TDD)

Fichier: `src/server.rs`

**Tests d'abord:**
```rust
#[tokio::test] async fn server_starts_and_accepts_connections() { ... }
#[tokio::test] async fn server_responds_to_ping() { ... }
#[tokio::test] async fn server_handles_unknown_request() { ... }
#[tokio::test] async fn server_concurrent_connections() { ... }
#[tokio::test] async fn server_graceful_shutdown() { ... }
```

**Implémentation:**
- `DaemonServer::new(config: DaemonConfig) -> Self`
- `DaemonServer::run(self) -> Result<()>` (main loop)
- UnixListener, accept loop, spawn handler per connection
- Request router: match `DaemonRequest` → dispatch to service
- Socket permission: 0o600 (owner-only)

#### 1.5 Lifecycle (TDD)

Fichier: `src/lifecycle.rs`

**Tests:**
```rust
#[test] fn pid_file_write_and_read() { ... }
#[test] fn pid_file_detects_stale() { ... }
#[test] fn socket_cleanup_on_start() { ... }
```

**Implémentation:**
- `write_pid_file(path)` / `read_pid_file(path)` / `is_process_alive(pid)`
- `cleanup_stale_socket(path)` — remove socket if PID dead
- `daemonize()` — double-fork pattern via `nix::unistd::fork/setsid`
- Signal handling: SIGTERM → graceful shutdown, SIGHUP → config reload

#### 1.6 Secrets Service (TDD)

Fichier: `src/services/secrets.rs`

**Tests:**
```rust
#[test] fn get_secret_from_env() { ... }
#[test] fn has_secret_empty_env_returns_false() { ... }
#[test] fn list_secrets_shows_all_providers() { ... }
// Note: keyring tests need #[cfg(not(ci))] or mock
```

**Implémentation:**
- `SecretService::new() -> Self`
- Reprendre la logique de `nika-engine/src/secrets/fallback.rs`
- Le daemon devient le SEUL accesseur keychain (comme avant spn-client)
- Les clients (nika run, nika ui) passent par le daemon pour les secrets

#### 1.7 CLI Commands

Fichier: `tools/nika/src/main.rs` + `tools/nika-cli/src/daemon.rs`

Ajouter `Commands::Daemon { action: DaemonAction }`:
```rust
enum DaemonAction {
    Start { foreground: bool },
    Stop,
    Restart,
    Status,
    Logs { follow: bool, lines: usize },
}
```

Implémentation:
- `start`: si `--foreground` → run server directly, sinon → `daemonize()` puis run
- `stop`: read PID file → send SIGTERM
- `status`: check PID alive + connect Ping
- `logs`: tail `~/.nika/daemon/nika.log`

#### 1.8 Doctor Integration

Fichier: `tools/nika-cli/src/doctor.rs`

Ajouter une section "Daemon" aux checks:
```rust
fn check_daemon() -> Vec<DiagnosticCheck> {
    vec![
        // Socket exists?
        // PID file valid?
        // Ping succeeds?
        // Version matches?
    ]
}
```

#### 1.9 Boot Integration

Fichier: `tools/nika-engine/src/runtime/boot.rs`

Phase 4 (secrets): si daemon disponible → utiliser DaemonClient, sinon → fallback env vars.

**COMMIT APRES CHAQUE SOUS-ETAPE (1.1 à 1.9).**

### Phase 2: Job Scheduler

**Prérequis:** Phase 1 complète et testée.

Ajouter deps: `rusqlite = { version = "0.32", features = ["bundled"] }`, `cron = "0.13"`

#### 2.1 Storage Layer (TDD)

Fichier: `src/storage.rs`

```rust
#[test] fn create_tables_idempotent() { ... }
#[test] fn insert_and_get_job() { ... }
#[test] fn list_jobs_by_state() { ... }
#[test] fn update_job_state() { ... }
#[test] fn job_history_tracking() { ... }
```

SQLite schema: voir `nika-daemon-architecture.md`

#### 2.2 Job Service (TDD)

Fichier: `src/services/jobs.rs`

```rust
#[tokio::test] async fn submit_job_creates_pending() { ... }
#[tokio::test] async fn run_job_spawns_nika_process() { ... }
#[tokio::test] async fn cancel_job_kills_process() { ... }
#[tokio::test] async fn cron_job_schedules_next_run() { ... }
#[tokio::test] async fn retry_on_failure() { ... }
#[tokio::test] async fn max_concurrent_jobs() { ... }
```

Jobs execute: `tokio::process::Command::new("nika").args(["run", workflow, "-y", "--json-output"])`

#### 2.3 CLI Commands

`nika job submit/list/status/cancel/retry/history`

#### 2.4 Integration Tests

Créer un workflow de test simple (`exec: echo hello`) et vérifier le cycle complet:
submit → pending → running → completed → history

### Phase 3: Watch + Cache

**Prérequis:** Phase 2 complète.

Ajouter deps: `notify = "7"`, `notify-debouncer-full = "0.4"`

#### 3.1 Watch Service (TDD)

```rust
#[tokio::test] async fn watch_detects_file_change() { ... }
#[tokio::test] async fn watch_debounces_rapid_changes() { ... }
#[tokio::test] async fn watch_filters_by_pattern() { ... }
#[tokio::test] async fn watch_submits_job_on_change() { ... }
```

#### 3.2 Cache Service (TDD)

```rust
#[test] fn cache_key_deterministic() { ... }
#[test] fn cache_hit_returns_value() { ... }
#[test] fn cache_miss_returns_none() { ... }
#[test] fn cache_ttl_expires() { ... }
#[test] fn cache_eviction_at_capacity() { ... }
#[tokio::test] async fn cache_persists_to_sqlite() { ... }
```

#### 3.3 Engine Integration

Fichier: `nika-engine/src/provider/mod.rs` (ou un wrapper)

Avant d'appeler le LLM: check cache via DaemonClient. Après réponse: store in cache.

```rust
if let Some(cached) = daemon_client.cache_get(&cache_key).await? {
    return Ok(cached);
}
let response = provider.infer(params).await?;
daemon_client.cache_set(&cache_key, &response, ttl).await?;
```

### Phase 4: Events + TUI Dashboard

#### 4.1 Event Bus

```rust
// Dans le daemon
pub struct EventBus {
    tx: broadcast::Sender<DaemonEvent>,
}

// Les workflows publient via IPC
// Le TUI souscrit via IPC
```

#### 4.2 TUI Job Dashboard

Nouveau panel dans la vue Control (view 3/x):
- Job list avec état (colored)
- Cache stats (hit ratio, size)
- Daemon status (uptime, services)

### Phase 5: Remaining Discovery Fixes

**Après le daemon**, attaquer les items restants du triage:

| ID | Status | Description |
|----|--------|-------------|
| C1 | DONE | LSP write lock |
| C2 | DONE | Daemon lock (legacy removed) |
| C3 | TODO | Replace git2+openssl with gix |
| C4 | DONE | charts-rs defaults |
| H1 | DONE | biased select! |
| H2 | TODO | Unbounded EventLog Vec |
| H3+H4 | TODO | reqwest 0.13 + TLS alignment |
| H5 | DONE | compute_layers tests |
| H6 | DONE | VALIDATOR_CACHE blake3 |
| H7 | DONE | Cache size cap |
| H8 | DONE | truncate_str Cow |
| H9 | TODO | serde_json in InvokeBox render |
| H10 | TODO | ContextAssembled token estimation |
| M13 | DONE | saturating_sub |

### Phase 6: ARCH-3 (Optional — if time)

Split NikaError 90 variants → domain enums. HIGH risk. Dedicated session recommended.

---

## RÈGLES CRITIQUES

1. **JAMAIS** `cargo test` sans `--lib` (keychain macOS popup)
2. **TDD**: tests FIRST, RED, GREEN, REFACTOR
3. **1 fix = 1 commit** avec format:
   ```
   type(scope): description

   Co-Authored-By: Nika 🦋 <nika@supernovae.studio>
   ```
4. **Push** tous les 3-5 commits
5. **Pre-commit hook**: `cargo clippy --all-targets --all-features -- -D warnings`
6. **nika-core** est ZERO I/O (pas de tokio, pas de reqwest)
7. **nika-daemon** ne dépend PAS de nika-engine (lightweight)
8. **NIKA-160-164** réservés pour nika-core ParseErrorKind
9. Utilise `CARGO_TARGET_DIR=/tmp/nika-target` si le target dir est corrompu
10. **Skills obligatoires**: brainstorming, rust, TDD, verification, code-review

## SKILLS À UTILISER

| Quand | Skill | Pourquoi |
|-------|-------|----------|
| Avant tout code Rust | `/spn-rust:rust` | Patterns Rust, routing vers skills spécialisées |
| Avant chaque phase | `/spn-powers:brainstorming` | Refine l'approche |
| Async patterns | `/spn-rust:rust-async` | Tokio, channels, select! |
| Avant d'écrire du code | `/spn-powers:test-driven-development` | TDD strict: RED → GREEN → REFACTOR |
| Avant de commit | `/spn-powers:verification-before-completion` | Tests + clippy |
| Après chaque phase | `/spn-powers:requesting-code-review` | Agent review du code |
| Si bug | `/spn-powers:systematic-debugging` | Trace root cause |
| Multi-fichiers | `/spn-powers:dispatching-parallel-agents` | Paralléliser les agents |
| Deps Rust | `/find-docs` | Context7 pour rusqlite, notify, nix, cron |
| Architecture | `/spn-rust:rust-architect` | Design decisions |
| Security | `/spn-rust:rust-security` | Socket perms, secret isolation |

## RESEARCH OBLIGATOIRE (Context7)

Avant d'écrire du code pour chaque crate externe, faire:
```
/find-docs rusqlite     → SQLite patterns, bundled, async
/find-docs notify       → File watcher, debounce, FSEvents
/find-docs nix          → fork, setsid, flock, signals, UnixStream
/find-docs cron         → Cron expression parsing
/find-docs keyring      → macOS keychain, entry API
```

## SUCCESS CRITERIA

Phase 1 (Foundation):
- [ ] `nika-daemon` crate existe dans le workspace (12ème crate)
- [ ] Protocol: DaemonRequest/Response sérialisent correctement
- [ ] Client: connect, ping, send/recv fonctionnent
- [ ] Server: accepte connections, route requests, répond
- [ ] Lifecycle: PID file, socket cleanup, daemonize, signal handling
- [ ] Secrets: get/set/has/list via daemon IPC
- [ ] `nika daemon start/stop/status` fonctionnent
- [ ] `nika doctor` check daemon health
- [ ] Boot Phase 4 utilise DaemonClient si disponible
- [ ] Tests: 50+ nouveaux tests dans nika-daemon
- [ ] Clippy: 0 warnings

Phase 2 (Jobs):
- [ ] SQLite storage: create/read/update/delete jobs
- [ ] `nika job submit workflow.nika.yaml` crée un job
- [ ] `nika job list` montre les jobs
- [ ] `nika job cancel <id>` tue le process
- [ ] Cron: `--cron "0 * * * *"` schedule récurrent
- [ ] Retry: `--max-retries 3` avec backoff
- [ ] Tests: 30+ tests job scheduler

Phase 3 (Watch + Cache):
- [ ] `nika watch .` détecte les changements et re-run
- [ ] Debounce 300ms sur les changements rapides
- [ ] LLM cache: hit/miss/TTL fonctionnent
- [ ] `nika cache stats` montre le hit ratio
- [ ] Cache persist across daemon restarts (SQLite)
- [ ] Tests: 20+ tests watch + cache

Phase 4 (Events + TUI):
- [ ] Event bus: publish/subscribe fonctionnent
- [ ] TUI Control view: job list panel
- [ ] Dashboard: cache stats, daemon status

Phase 5 (Discovery):
- [ ] C3: git2 remplacé par gix (ou git CLI)
- [ ] H2: EventLog borné
- [ ] H3+H4: reqwest 0.13 + TLS unifié
- [ ] H9: serde_json caché dans InvokeBox
- [ ] H10: token estimation sans sérialisation
