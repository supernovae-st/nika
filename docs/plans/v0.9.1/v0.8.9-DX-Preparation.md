# v0.8.9 — DX Infrastructure Preparation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Prepare all Developer Experience (DX) infrastructure for v0.9.x implementation — update skills, rules, READMEs, settings, hooks, and scripts without breaking existing pipelines.

**Architecture:** Pre-flight checklist approach — verify, update, test each DX component independently before starting v0.9.0 work.

**Tech Stack:** Bash, YAML, Markdown, Claude Code DX

---

## Overview

```
╔═══════════════════════════════════════════════════════════════════════════════╗
║  v0.8.9 DX PREPARATION — PRE-FLIGHT FOR v0.9.x                                ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║  GOAL: Ensure all DX infrastructure is ready for v0.9.x development          ║
║                                                                               ║
║  ┌─────────────────────────────────────────────────────────────────────────┐  ║
║  │ PROBLEM: DX files reference v0.7.2/v0.8.0 — need v0.9.x readiness      │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                                                               ║
║  Files to Update:                                                             ║
║  ├── Skills (8 files)    → Version references, feature lists                  ║
║  ├── Rules (6 files)     → ADR references, patterns                           ║
║  ├── Hooks (12 files)    → Validation scripts, compatibility                  ║
║  ├── Settings (4 files)  → Version numbers, test counts                       ║
║  ├── READMEs (3 files)   → Feature lists, roadmap links                       ║
║  └── Scripts (5 files)   → Pipeline compatibility                             ║
║                                                                               ║
║  CRITICAL: NO BREAKING CHANGES TO EXISTING PIPELINES                          ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝
```

---

## Task Index

| Task | Component | Files | Tests | Effort |
|------|-----------|-------|-------|--------|
| 1 | Skills INDEX | 1 | 3 | 15 min |
| 2 | Individual Skills | 8 | 8 | 30 min |
| 3 | Rules Updates | 6 | 6 | 30 min |
| 4 | Settings JSON | 4 | 4 | 15 min |
| 5 | Hooks Audit | 12 | 12 | 45 min |
| 6 | READMEs Update | 3 | 3 | 30 min |
| 7 | Scripts Validation | 5 | 5 | 30 min |
| 8 | Pipeline Smoke Test | - | 15 | 30 min |

**Totals:** 39 files, 56 tests, ~4 hours

---

## Task 1: Update Skills INDEX

**Files:**
- Modify: `nika/.claude/skills/INDEX.md`

**Step 1: Read current INDEX.md**

```bash
cat nika/.claude/skills/INDEX.md
```

**Step 2: Update version and features**

Update from v0.7.2 → v0.8.0 with v0.9.x preview:

```markdown
# Nika Skills Index

**Nika Version:** v0.8.0 | **5 verbs** | Studio DX | rig-core v0.31 | 6 LLM providers

## v0.9.x Preview Features

| Feature | Version | Description |
|---------|---------|-------------|
| StableGraph | v0.9.0 | Stable NodeIndex for @mention references |
| ChatWorkflow | v0.9.1 | Persistent chat DAG structure |
| @mentions | v0.9.2 | Parser for @N message references |
| nika:* tools | v0.9.3 | 6 built-in tools (nika:edit, nika:search, etc.) |
| DAG Panel | v0.9.4 | TUI visualization of chat workflow |
| Polish | v0.9.5 | Animations, export, final touches |
```

**Step 3: Verify skill list completeness**

Ensure all 8 skills are documented:
- nika-yaml
- nika-arch
- nika-run
- nika-diagnose
- nika-debug
- nika-binding
- workflow-validate
- nika-spec

**Step 4: Commit**

```bash
git add nika/.claude/skills/INDEX.md
git commit -m "docs(dx): update skills INDEX for v0.8.0 with v0.9.x preview"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| Version check | `grep "v0.8.0" INDEX.md` | Match found |
| Skill count | `grep -c "^\| \*\*" INDEX.md` | 8 skills |
| Preview section | `grep "v0.9.x Preview" INDEX.md` | Section exists |

---

## Task 2: Update Individual Skills

**Files:**
- Modify: `nika/.claude/skills/nika-yaml/SKILL.md`
- Modify: `nika/.claude/skills/nika-arch/SKILL.md`
- Modify: `nika/.claude/skills/nika-run/SKILL.md`
- Modify: `nika/.claude/skills/nika-debug/SKILL.md`
- Modify: `nika/.claude/skills/nika-diagnose/SKILL.md`
- Modify: `nika/.claude/skills/nika-binding/SKILL.md`
- Modify: `nika/.claude/skills/nika-spec/SKILL.md`
- Modify: `nika/.claude/skills/workflow-validate/SKILL.md`

**Step 1: Check each skill for version references**

```bash
grep -r "v0.7" nika/.claude/skills/
grep -r "v0.8.0" nika/.claude/skills/
```

**Step 2: Update version references in each skill**

For each skill file, update:
- Version number: v0.7.x → v0.8.0
- Test count: 2,323 → 1,902 (correct count)
- Feature list: Add v0.8.0 features (Edit History, Sessions, etc.)

**Step 3: Add v0.9.x awareness to nika-arch skill**

Add to `nika-arch/SKILL.md`:

```markdown
## v0.9.x Architecture Changes (Preview)

```
src/
├── dag/
│   ├── mod.rs
│   ├── flow_graph.rs      # Current: DiGraph
│   └── stable_graph.rs    # v0.9.0: StableGraph wrapper
├── chat/                  # v0.9.1: NEW module
│   ├── workflow.rs        # ChatWorkflow struct
│   ├── mention.rs         # v0.9.2: @mention parser
│   └── tools.rs           # v0.9.3: nika:* builtins
└── tui/
    └── widgets/
        ├── dag_panel.rs   # v0.9.4: DAG visualization
        └── task_box/      # v0.9.4: Verb-specific widgets
```
```

**Step 4: Commit**

```bash
git add nika/.claude/skills/
git commit -m "docs(dx): update all skills for v0.8.0 and v0.9.x preview"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| No v0.7 refs | `grep -r "v0.7" skills/` | No matches |
| v0.8.0 present | `grep -r "v0.8.0" skills/` | Multiple matches |
| Arch preview | `grep "v0.9.x" nika-arch/` | Section exists |

---

## Task 3: Update Rules

**Files:**
- Modify: `nika/tools/nika/.claude/rules/testing.md`
- Modify: `nika/tools/nika/.claude/rules/error-handling.md`
- Modify: `nika/tools/nika/.claude/rules/PERFORMANCE.md`
- Modify: `nika/tools/nika/.claude/rules/adr/adr-001-5-semantic-verbs.md`
- Modify: `dx/.claude/rules/nika.md`
- Modify: `dx/.claude/rules/architecture.md`

**Step 1: Audit rules for outdated references**

```bash
grep -r "v0.7" nika/tools/nika/.claude/rules/
grep -r "v0.8.0" dx/.claude/rules/
```

**Step 2: Update dx/.claude/rules/nika.md**

Update version and test count:
```markdown
**v0.8.0** | rig-core v0.31 | 6 providers + Full Streaming | 1,902 tests
```

**Step 3: Add v0.9.x ADR reference stubs**

Create placeholder for future ADRs:

```markdown
## v0.9.x ADRs (Planned)

| ADR | Topic | Status |
|-----|-------|--------|
| ADR-007 | StableGraph migration | Planned (v0.9.0) |
| ADR-008 | ChatWorkflow structure | Planned (v0.9.1) |
| ADR-009 | @mention syntax | Planned (v0.9.2) |
| ADR-010 | nika:* builtin tools | Planned (v0.9.3) |
```

**Step 4: Commit**

```bash
git add nika/tools/nika/.claude/rules/ dx/.claude/rules/
git commit -m "docs(dx): update rules for v0.8.0, add v0.9.x ADR stubs"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| Version in nika.md | `grep "v0.8.0" nika.md` | Match found |
| Test count | `grep "1,902" nika.md` | Match found |
| ADR stubs | `grep "ADR-007" nika.md` | Match found |

---

## Task 4: Update Settings JSON

**Files:**
- Modify: `nika/.claude/settings.json`
- Modify: `nika/tools/nika/.claude/settings.json`
- Modify: `dx/.claude/settings.json`
- Modify: `dx/.claude/settings.local.json`

**Step 1: Read current settings**

```bash
cat nika/tools/nika/.claude/settings.json
```

**Step 2: Update version numbers**

Update `nika/tools/nika/.claude/settings.json`:

```json
{
  "project": "nika",
  "version": "0.8.0",
  "primaryLanguage": "rust",
  "testCommand": "cargo nextest run",
  "buildCommand": "cargo build --release",
  "lintCommand": "cargo clippy -- -D warnings",
  "formatCommand": "cargo fmt --check",
  "notes": {
    "v0.8.0": "Studio DX complete - Edit History, Sessions, Solarized, Config",
    "v0.9.x": "Chat-as-DAG architecture (StableGraph, ChatWorkflow, @mentions, nika:* tools)",
    "tests": "1,902 tests passing",
    "providers": ["Claude", "OpenAI", "Mistral", "Groq", "DeepSeek", "Ollama"]
  }
}
```

**Step 3: Verify JSON syntax**

```bash
jq . nika/tools/nika/.claude/settings.json
```

**Step 4: Commit**

```bash
git add nika/.claude/settings.json nika/tools/nika/.claude/settings.json
git commit -m "chore(dx): update settings.json to v0.8.0"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| JSON valid | `jq . settings.json` | Valid output |
| Version 0.8.0 | `jq .version settings.json` | "0.8.0" |
| v0.9.x note | `jq .notes settings.json` | Contains "v0.9.x" |

---

## Task 5: Audit Hooks

**Files:**
- Audit: `nika/.claude/hooks/*.sh` (6 files)
- Audit: `dx/.claude/hooks/nika/*.sh` (6 files)

**Step 1: List all hooks**

```bash
ls -la nika/.claude/hooks/
ls -la dx/.claude/hooks/nika/
```

**Step 2: Test each hook individually**

```bash
# Test nika-health-check
bash nika/.claude/hooks/nika-health-check.sh

# Test yaml-validate
bash nika/.claude/hooks/yaml-validate.sh examples/test-workflow.nika.yaml

# Test verify-alignment
bash nika/.claude/hooks/verify-alignment.sh

# Test auto-load-spec
bash nika/.claude/hooks/auto-load-spec.sh

# Test check-git-commit
bash nika/.claude/hooks/check-git-commit.sh

# Test nika-pre-commit
bash nika/.claude/hooks/nika-pre-commit.sh
```

**Step 3: Fix any broken hooks**

Common issues:
- Path references to old locations
- Version checks that fail on v0.8.0
- Missing dependencies

**Step 4: Document hook purposes**

Create `nika/.claude/hooks/README.md`:

```markdown
# Nika Hooks

| Hook | Event | Purpose |
|------|-------|---------|
| nika-health-check.sh | SessionStart | Verify Rust toolchain |
| yaml-validate.sh | PostToolUse | Validate .nika.yaml files |
| verify-alignment.sh | PostToolUse | Check spec/code alignment |
| auto-load-spec.sh | UserPromptSubmit | Load relevant spec context |
| check-git-commit.sh | PreToolUse | Validate commit messages |
| nika-pre-commit.sh | PreToolUse | Run tests before commit |
```

**Step 5: Commit**

```bash
git add nika/.claude/hooks/
git commit -m "chore(dx): audit and document hooks for v0.9.x readiness"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| health-check | `bash nika-health-check.sh` | Exit 0 |
| yaml-validate | `bash yaml-validate.sh test.yaml` | Exit 0 or clear error |
| All hooks executable | `ls -la *.sh` | -rwxr-xr-x |

---

## Task 6: Update READMEs

**Files:**
- Modify: `nika/README.md`
- Modify: `nika/tools/nika/README.md`
- Modify: `nika/CLAUDE.md`

**Step 1: Read current READMEs**

```bash
head -100 nika/README.md
head -100 nika/tools/nika/README.md
```

**Step 2: Update version references**

In `nika/README.md`, update:
- Current version: v0.8.0
- Test count: 1,902
- Feature list: Add Studio DX features

**Step 3: Add v0.9.x roadmap section**

```markdown
## Roadmap

### v0.9.x — Chat-as-DAG Architecture

| Version | Focus | Key Feature |
|---------|-------|-------------|
| v0.9.0 | StableGraph | Stable NodeIndex after deletion |
| v0.9.1 | ChatWorkflow | Persistent chat DAG |
| v0.9.2 | @mentions | @N message reference parser |
| v0.9.3 | Builtin Tools | 6 nika:* tools |
| v0.9.4 | DAG Panel | TUI visualization |
| v0.9.5 | Polish | Animations, export |

See `docs/plans/v0.9.1/` for detailed plans.
```

**Step 4: Update CLAUDE.md**

Update the Auto-Imported Context section to reference v0.9.x plans.

**Step 5: Commit**

```bash
git add nika/README.md nika/tools/nika/README.md nika/CLAUDE.md
git commit -m "docs: update READMEs for v0.8.0 with v0.9.x roadmap"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| Version in README | `grep "v0.8.0" README.md` | Match found |
| Roadmap section | `grep "v0.9.x" README.md` | Section exists |
| Test count | `grep "1,902" README.md` | Match found |

---

## Task 7: Validate Scripts

**Files:**
- Audit: `nika/.claude/scripts/nika-validate.sh`
- Audit: `scripts/verify.sh`
- Audit: `scripts/release.sh`
- Audit: `scripts/validate-versions.sh`
- Audit: `nika/tools/nika/examples/demo.sh`

**Step 1: List all scripts**

```bash
find nika -name "*.sh" -not -path "*/node_modules/*"
find scripts -name "*.sh"
```

**Step 2: Test each script with --help or dry-run**

```bash
# Test nika-validate
bash nika/.claude/scripts/nika-validate.sh --help

# Test verify
bash scripts/verify.sh --dry-run

# Test release
bash scripts/release.sh --help
```

**Step 3: Fix any path issues**

Common fixes:
- Update relative paths
- Fix Cargo.toml references
- Update version patterns

**Step 4: Add v0.9.x compatibility checks**

Add to `scripts/verify.sh`:

```bash
# v0.9.x readiness check
check_v09x_readiness() {
  echo "Checking v0.9.x readiness..."
  # Verify StableGraph import ready
  grep -q "petgraph" nika/tools/nika/Cargo.toml || warn "petgraph not in deps"
  # Verify chat module placeholder
  [ -d "nika/tools/nika/src/chat" ] || info "chat/ module not yet created (v0.9.1)"
}
```

**Step 5: Commit**

```bash
git add scripts/ nika/.claude/scripts/
git commit -m "chore(dx): validate scripts for v0.9.x compatibility"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| nika-validate | `bash nika-validate.sh` | Exit 0 |
| verify.sh | `bash verify.sh --dry-run` | No errors |
| All executable | `find . -name "*.sh" -exec test -x {} \;` | All pass |

---

## Task 8: Pipeline Smoke Test

**Files:**
- None (integration test)

**Step 1: Run full test suite**

```bash
cd nika/tools/nika
cargo test --lib
cargo clippy -- -D warnings
cargo fmt --check
```

**Step 2: Run CI simulation**

```bash
# Simulate GitHub Actions
cargo build --release
cargo nextest run
```

**Step 3: Test hook chain**

```bash
# Simulate session start
bash nika/.claude/hooks/nika-health-check.sh

# Simulate edit
echo "test" >> /tmp/test.nika.yaml
bash nika/.claude/hooks/yaml-validate.sh /tmp/test.nika.yaml
rm /tmp/test.nika.yaml
```

**Step 4: Verify DX tools work**

```bash
# Test CLI help
cargo run -- --help
cargo run -- chat --help
cargo run -- studio --help
```

**Step 5: Document any issues found**

Create `.claude/reports/dx-audit-$(date +%Y-%m-%d).md` if issues found.

**Step 6: Final commit**

```bash
git add -A
git commit -m "chore(dx): complete v0.8.9 DX preparation for v0.9.x

- Updated all skills to v0.8.0 with v0.9.x preview
- Updated rules with v0.9.x ADR stubs
- Updated settings.json to v0.8.0
- Audited and documented all hooks
- Updated READMEs with roadmap
- Validated all scripts
- Pipeline smoke test passing

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Nika <agent@nika.sh>"
```

**Tests:**
| Test | Command | Expected |
|------|---------|----------|
| All tests pass | `cargo test --lib` | 1,902 passing |
| No warnings | `cargo clippy -- -D warnings` | 0 warnings |
| Format clean | `cargo fmt --check` | No diff |
| Build succeeds | `cargo build --release` | Success |
| CLI works | `cargo run -- --help` | Help output |

---

## Comprehensive Testing Protocol

### Unit Tests Table

| Category | Test Names | Count |
|----------|------------|-------|
| **Skills** | `test_index_version`, `test_skill_count`, `test_preview_section` | 3 |
| **Skills Content** | `test_skill_*_version` (8 skills) | 8 |
| **Rules** | `test_rule_*_updated` (6 rules) | 6 |
| **Settings** | `test_settings_*_valid` (4 files) | 4 |
| **Hooks** | `test_hook_*_executable` (12 hooks) | 12 |
| **READMEs** | `test_readme_*_version` (3 files) | 3 |
| **Scripts** | `test_script_*_works` (5 scripts) | 5 |
| **Pipeline** | `test_build`, `test_test`, `test_lint`, etc. | 15 |
| **Total** | | **56** |

### Verification Checklist

```bash
# Run all verification
./scripts/verify.sh

# Expected output:
# ✓ Skills INDEX updated to v0.8.0
# ✓ All 8 skills have v0.8.0 references
# ✓ Rules updated with v0.9.x ADR stubs
# ✓ Settings JSON valid and v0.8.0
# ✓ All 12 hooks executable and working
# ✓ READMEs have roadmap section
# ✓ Scripts pass dry-run
# ✓ 1,902 tests passing
# ✓ 0 clippy warnings
# ✓ Format check clean
```

### Live Test Scenarios

**Scenario 1: New Session Start**
```bash
# Start fresh Claude Code session
# Expected: nika-health-check.sh runs, reports v0.8.0 ready
```

**Scenario 2: Edit YAML Workflow**
```bash
# Edit examples/test-workflow.nika.yaml
# Expected: yaml-validate.sh runs, reports valid
```

**Scenario 3: Read Skill**
```bash
# /nika-arch
# Expected: Shows v0.8.0 architecture with v0.9.x preview
```

**Scenario 4: Pre-commit Hook**
```bash
git add .
git commit -m "test"
# Expected: nika-pre-commit.sh runs, tests pass
```

---

## Phase Completion Checkpoint

### Step 1: Full Test Suite

```bash
cd nika/tools/nika
cargo test --lib
# Expected: 1,902 tests passing
```

### Step 2: Commit Phase

```bash
git add -A
git commit -m "$(cat <<'EOF'
chore(dx): complete v0.8.9 DX preparation for v0.9.x

- Skills: Updated INDEX and all 8 skills to v0.8.0
- Rules: Added v0.9.x ADR stubs
- Settings: Updated version to 0.8.0
- Hooks: Audited and documented all 12 hooks
- READMEs: Added v0.9.x roadmap section
- Scripts: Validated pipeline compatibility

Total: 39 files updated, 56 verification tests

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Nika <agent@nika.sh>
EOF
)"
```

### Step 3: Push & Verify CI

```bash
git push origin main
gh run watch
# Expected: All CI checks pass
```

### Step 4: Tag Release

```bash
git tag -a v0.8.9 -m "DX preparation for v0.9.x"
git push origin v0.8.9
```

### Step 5: Verify Tag

```bash
git tag -l "v0.8.*"
# Expected: v0.8.0, v0.8.9 listed
```

### Step 6: Update CHANGELOG

Add to `CHANGELOG.md`:

```markdown
## [0.8.9] - 2026-02-24

### Changed
- Updated all skills to v0.8.0 with v0.9.x preview
- Updated rules with v0.9.x ADR stubs
- Updated settings.json version
- Audited and documented all hooks
- Updated READMEs with v0.9.x roadmap
- Validated all scripts for v0.9.x compatibility

### Documentation
- Added v0.8.9-DX-Preparation.md plan
- Updated INDEX.md with DX preparation task
```

---

## Success Metrics

| Metric | Target | Status |
|--------|--------|--------|
| Skills updated | 8/8 | ⏳ |
| Rules updated | 6/6 | ⏳ |
| Settings updated | 4/4 | ⏳ |
| Hooks working | 12/12 | ⏳ |
| READMEs updated | 3/3 | ⏳ |
| Scripts validated | 5/5 | ⏳ |
| Tests passing | 1,902 | ⏳ |
| Clippy warnings | 0 | ⏳ |
| CI green | ✓ | ⏳ |

---

## File Summary

```
Files to Update (39 total):
├── Skills (9 files)
│   ├── nika/.claude/skills/INDEX.md
│   ├── nika/.claude/skills/nika-yaml/SKILL.md
│   ├── nika/.claude/skills/nika-arch/SKILL.md
│   ├── nika/.claude/skills/nika-run/SKILL.md
│   ├── nika/.claude/skills/nika-debug/SKILL.md
│   ├── nika/.claude/skills/nika-diagnose/SKILL.md
│   ├── nika/.claude/skills/nika-binding/SKILL.md
│   ├── nika/.claude/skills/nika-spec/SKILL.md
│   └── nika/.claude/skills/workflow-validate/SKILL.md
│
├── Rules (6 files)
│   ├── nika/tools/nika/.claude/rules/testing.md
│   ├── nika/tools/nika/.claude/rules/error-handling.md
│   ├── nika/tools/nika/.claude/rules/PERFORMANCE.md
│   ├── nika/tools/nika/.claude/rules/adr/adr-001-5-semantic-verbs.md
│   ├── dx/.claude/rules/nika.md
│   └── dx/.claude/rules/architecture.md
│
├── Settings (4 files)
│   ├── nika/.claude/settings.json
│   ├── nika/tools/nika/.claude/settings.json
│   ├── dx/.claude/settings.json
│   └── dx/.claude/settings.local.json
│
├── Hooks (12 files)
│   ├── nika/.claude/hooks/nika-health-check.sh
│   ├── nika/.claude/hooks/yaml-validate.sh
│   ├── nika/.claude/hooks/verify-alignment.sh
│   ├── nika/.claude/hooks/auto-load-spec.sh
│   ├── nika/.claude/hooks/check-git-commit.sh
│   ├── nika/.claude/hooks/nika-pre-commit.sh
│   ├── dx/.claude/hooks/nika/yaml-validate.sh
│   ├── dx/.claude/hooks/nika/nika-pre-commit.sh
│   ├── dx/.claude/hooks/nika/verify-alignment.sh
│   ├── dx/.claude/hooks/nika/auto-load-spec.sh
│   ├── dx/.claude/hooks/nika/check-git-commit.sh
│   └── dx/.claude/hooks/nika/nika-health-check.sh
│
├── READMEs (3 files)
│   ├── nika/README.md
│   ├── nika/tools/nika/README.md
│   └── nika/CLAUDE.md
│
├── Scripts (5 files)
│   ├── nika/.claude/scripts/nika-validate.sh
│   ├── scripts/verify.sh
│   ├── scripts/release.sh
│   ├── scripts/validate-versions.sh
│   └── nika/tools/nika/examples/demo.sh
│
└── NEW Files (2 files)
    ├── nika/.claude/hooks/README.md
    └── .claude/reports/dx-audit-2026-02-24.md (if issues)
```

---

## Dependencies

This plan has **NO external dependencies** — it only updates documentation and configuration files.

**Prerequisite:** v0.8.0 must be released (✅ already done)

**Blocks:** v0.9.0-StableGraph.md (should wait for DX preparation)

---

## Risk Assessment

| Risk | Impact | Mitigation |
|------|--------|------------|
| Hook breaks CI | HIGH | Test each hook individually before commit |
| Wrong version in file | LOW | Grep verification for each file |
| Script path changes | MEDIUM | Use relative paths, test in isolation |
| README outdated | LOW | Cross-reference with CHANGELOG |

---

## Execution Notes

**Order matters:** Execute tasks 1-7 in order, then task 8 as final validation.

**Safe to parallelize:** Tasks 1-3 (documentation updates) can be done in parallel.

**Must be sequential:** Tasks 5-8 depend on earlier tasks.

**Rollback:** If issues found, revert with `git checkout HEAD~1 -- <file>`.
