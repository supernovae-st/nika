schema: "nika/workflow@0.12"
provider: mock

tasks:
  - id: greeting
    infer:
      prompt: "Say hello in French"
    output:
      format: text

  - id: response
    depends_on: [greeting]
    with:
      msg: $greeting
    infer:
      prompt: "Reply to: {{with.msg}}"
