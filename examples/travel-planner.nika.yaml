# Complex travel planner - multiple fan-in/fan-out patterns
#
# DAG STRUCTURE (18 tasks, 12 flows, 7 phases):
#
#   PHASE 1: Input (5 exec tasks)
#   ┌─────────┬─────────────┬──────────┬────────┬───────────┐
#   │get_date │ destination │ duration │ budget │ interests │
#   └────┬────┴──────┬──────┴────┬─────┴───┬────┴─────┬─────┘
#        │           │           │         │          │
#        ▼           ▼           ▼         ▼          ▼
#   PHASE 2: Research (fan-out)       PHASE 3: Activities (fan-out)
#   ┌────────┬────────┬───────┬────┐  ┌───────────┬──────────┬──────┬────────┐
#   │weather │flights │hotels │visa│  │food_spots │ cultural │ tech │nightlife│
#   └───┬────┴───┬────┴───┬───┴──┬─┘  └─────┬─────┴────┬─────┴──┬───┴───┬────┘
#       │        │        │      │          │          │        │       │
#       └────────┴────────┴──────┘          └──────────┴────────┴───────┘
#                    │                                  │
#                    ▼                                  ▼
#   PHASE 4: Consolidation (fan-in)
#   ┌──────────────────┐                ┌────────────────────┐
#   │ logistics_summary│                │ activities_summary │
#   └────────┬─────────┘                └──────────┬─────────┘
#            │                                     │
#            ├─────────────────┬───────────────────┤
#            ▼                 ▼                   ▼
#   PHASE 5+6: Planning (parallel)
#   ┌──────────────────┐      ┌──────────────────┐
#   │ daily_itinerary  │      │ budget_breakdown │
#   └────────┬─────────┘      └────────┬─────────┘
#            │                         │
#            └───────────┬─────────────┘
#                        ▼
#   PHASE 7: Final (fan-in)
#   ┌──────────────────────────────────────┐
#   │            final_report              │
#   └──────────────────────────────────────┘
#
schema: "nika/workflow@0.12"

provider: openai
model: gpt-4o-mini

tasks:
  # ═══════════════════════════════════════════════════════════════
  # PHASE 1: Input gathering (sequential)
  # ═══════════════════════════════════════════════════════════════

  - id: get_date
    exec:
      command: "date '+%Y-%m-%d'"

  - id: destination
    exec:
      command: "echo 'Tokyo, Japan'"

  - id: duration
    exec:
      command: "echo '5 days'"

  - id: budget
    exec:
      command: "echo '2000 EUR'"

  - id: interests
    exec:
      command: "echo 'food, temples, technology, nightlife'"

  # ═══════════════════════════════════════════════════════════════
  # PHASE 2: Parallel research (fan-out from destination)
  # ═══════════════════════════════════════════════════════════════

  - id: weather
    depends_on: [destination, get_date, duration]
    with:
      date: get_date
      dest: destination
      days: duration
    infer:
      prompt: |
        Current date: {{with.date}}
        Destination: {{with.dest}}
        Duration: {{with.days}}

        Provide a brief weather forecast for this trip.
        Include: temperature range, rain probability, what to pack.
        Keep it to 3-4 sentences.

  - id: flights
    depends_on: [destination, duration, budget]
    with:
      dest: destination
      days: duration
      money: budget
    infer:
      prompt: |
        Destination: {{with.dest}}
        Duration: {{with.days}}
        Budget: {{with.money}}

        Suggest flight options (economy class).
        Include: typical price range, best airlines, flight duration.
        Keep it to 3-4 sentences.

  - id: hotels
    depends_on: [destination, duration, budget]
    with:
      dest: destination
      days: duration
      money: budget
      hobbies: interests
    infer:
      prompt: |
        Destination: {{with.dest}}
        Duration: {{with.days}}
        Budget: {{with.money}}
        Interests: {{with.hobbies}}

        Suggest 2-3 neighborhoods to stay in and hotel budget per night.
        Keep it to 4-5 sentences.

  - id: visa_info
    depends_on: [destination]
    with:
      dest: destination
    infer:
      prompt: |
        Destination: {{with.dest}}

        Brief visa requirements for EU citizens.
        Keep it to 2-3 sentences.

  # ═══════════════════════════════════════════════════════════════
  # PHASE 3: Activity planning (fan-out from interests)
  # ═══════════════════════════════════════════════════════════════

  - id: food_spots
    depends_on: [budget, interests, destination]
    with:
      dest: destination
      money: budget
    infer:
      prompt: |
        Destination: {{with.dest}}
        Interest: Food & Cuisine
        Budget: {{with.money}}

        Recommend 5 must-try food experiences (mix of cheap eats and special dinners).
        Keep it brief, one line per recommendation.

  - id: cultural_sites
    depends_on: [interests, destination]
    with:
      dest: destination
    infer:
      prompt: |
        Destination: {{with.dest}}
        Interest: Temples & Culture

        Recommend 5 must-visit temples and cultural sites.
        Keep it brief, one line per recommendation.

  - id: tech_attractions
    depends_on: [interests, destination]
    with:
      dest: destination
    infer:
      prompt: |
        Destination: {{with.dest}}
        Interest: Technology & Innovation

        Recommend 5 tech-related attractions (robot restaurants, electronics districts, etc).
        Keep it brief, one line per recommendation.

  - id: nightlife
    depends_on: [interests, destination]
    with:
      dest: destination
    infer:
      prompt: |
        Destination: {{with.dest}}
        Interest: Nightlife

        Recommend 5 nightlife spots (bars, clubs, entertainment districts).
        Keep it brief, one line per recommendation.

  # ═══════════════════════════════════════════════════════════════
  # PHASE 4: Consolidation (fan-in)
  # ═══════════════════════════════════════════════════════════════

  - id: activities_summary
    depends_on: [food_spots, cultural_sites, tech_attractions, nightlife]
    with:
      food: food_spots
      culture: cultural_sites
      tech: tech_attractions
      night: nightlife
    infer:
      prompt: |
        Combine these activity recommendations into a cohesive list:

        FOOD:
        {{with.food}}

        CULTURE:
        {{with.culture}}

        TECH:
        {{with.tech}}

        NIGHTLIFE:
        {{with.night}}

        Create a TOP 10 must-do list, mixing categories. One line per item.

  - id: logistics_summary
    depends_on: [weather, flights, hotels, visa_info]
    with:
      weather_info: weather
      flight_info: flights
      hotel_info: hotels
      visa: visa_info
    infer:
      prompt: |
        Summarize the travel logistics:

        WEATHER:
        {{with.weather_info}}

        FLIGHTS:
        {{with.flight_info}}

        HOTELS:
        {{with.hotel_info}}

        VISA:
        {{with.visa}}

        Create a brief "Before You Go" checklist. Keep it to 5-6 bullet points.

  # ═══════════════════════════════════════════════════════════════
  # PHASE 5: Day-by-day planning (sequential from summaries)
  # ═══════════════════════════════════════════════════════════════

  - id: daily_itinerary
    depends_on: [duration, activities_summary, weather, destination]
    with:
      dest: destination
      days: duration
      money: budget
      weather_info: weather
      activities: activities_summary
    infer:
      prompt: |
        Create a day-by-day itinerary for this trip:

        TRIP INFO:
        - Destination: {{with.dest}}
        - Duration: {{with.days}}
        - Budget: {{with.money}}

        WEATHER CONSIDERATIONS:
        {{with.weather_info}}

        TOP ACTIVITIES:
        {{with.activities}}

        Create a {{with.days}} itinerary with morning, afternoon, evening for each day.
        Keep each day to 3-4 lines.

  # ═══════════════════════════════════════════════════════════════
  # PHASE 6: Budget breakdown (parallel with itinerary)
  # ═══════════════════════════════════════════════════════════════

  - id: budget_breakdown
    depends_on: [budget, destination, logistics_summary]
    with:
      money: budget
      days: duration
      dest: destination
      logistics: logistics_summary
    infer:
      prompt: |
        Create a budget breakdown:

        - Total budget: {{with.money}}
        - Duration: {{with.days}}
        - Destination: {{with.dest}}

        LOGISTICS:
        {{with.logistics}}

        Estimate costs for: flights, accommodation, food, activities, transport, misc.
        Show daily average and total. Keep it brief.

  # ═══════════════════════════════════════════════════════════════
  # PHASE 7: Final report (fan-in of everything)
  # ═══════════════════════════════════════════════════════════════

  - id: final_report
    depends_on: [logistics_summary, daily_itinerary, budget_breakdown, get_date, destination, duration, budget]
    with:
      dest: destination
      days: duration
      money: budget
      date: get_date
      logistics: logistics_summary
      itinerary: daily_itinerary
      costs: budget_breakdown
    infer:
      prompt: |
        Create the FINAL TRAVEL PLAN:

        ═══════════════════════════════════════
        TRIP: {{with.dest}} | {{with.days}} | {{with.money}}
        DATE: {{with.date}}
        ═══════════════════════════════════════

        BEFORE YOU GO:
        {{with.logistics}}

        DAILY ITINERARY:
        {{with.itinerary}}

        BUDGET:
        {{with.costs}}

        Format this as a clean, professional travel plan.
        Add a brief "Pro Tips" section at the end (3-4 tips).
