# SPDX-License-Identifier: Apache-2.0astr (diff, blame, log)
        - "nika:done"              # completion sentinel · lets the agent end th: falasml-language-server: $schema=  - `fail_fast: false` · process everyog)
        - "nika:done"              # completion sentinel · lets the agent end th: falasml-language-server: $schema=  - `fail_fast: false` · process every locqle even if one fails
#   - the task `.output` is the ARRAY of pe intoslate.ouslate.output )}}"
