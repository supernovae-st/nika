# Known .expect() violations pre-dating Batch H+ (2026-04-15).
# Each entry: file path — ratchet skips these lines until fixed.
# Remove entries as violations are resolved.
#
# reason: semaphore acquire in catalog-verify CLI tool (not library code).
# The semaphore is created locally and never closed, so acquire cannot
# fail at runtime. Will be replaced with ? propagation in Session 4.
crates/nika-catalog-verify/src/main.rs
#
# reason: proptest helpers inside #[cfg(all(test, feature = "..."))] modules.
# The bash strip_test_items heuristic cannot parse #[cfg(all(test, ...))].
# These .expect("valid regex") are on hardcoded compile-time regexes that
# cannot fail. The real source of truth is clippy expect_used (which
# correctly scopes these as test code).
crates/nika-catalog/src/data/models.rs
#
# reason: whole-file #[cfg(test)] modules (the cfg lives at the lib.rs mod
# declaration — `#[cfg(test)] mod parity_tests;` — so neither the bash
# strip_test_items heuristic nor a per-file scan can see the test scope).
# Same class as the nika-catalog models.rs entry above. The files carry
# file-level #![allow(clippy::unwrap_used, clippy::expect_used)] and clippy
# expect_used (the real source of truth) scopes them as test code.
crates/nika-providers/src/parity_tests.rs
crates/nika-providers/src/test_support.rs
