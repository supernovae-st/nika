# Known fn-length violations pre-dating Batch H+ (2026-04-15).
# Each entry: file:fn_name — ratchet skips these until fixed.
# Remove entries as violations are resolved.
#
# reason: session_2b_per_rule_provenance is a data-driven test helper
# that generates per-rule provenance assertions. Splitting it would obscure
# the test intent. Re-baselined 2026-06-10 · 293 lines (was documented 165 ·
# +78% silent growth under exemption · architecture review catch). HARD
# CEILING · 350 lines — past that the entry MUST be split, not re-baselined.
crates/nika-catalog/src/data/models.rs:session_2b_per_rule_provenance

# reason: the Compile core's long walks pre-date its descent to nika-compile
# (ADR-137 · 2026-09-21): one clause walk per policy family (read_one ·
# read_clause · read · the reader is FROZEN · in nika-compile-reader since
# ADR-138), one resolution ladder read top to
# bottom (compile_with_cognition · sampled · merge · create), one validation walk
# over one closed shape (typed_rule · feasibility · bind). Tracked for the
# season-2 debt wave; HARD CEILING · the length measured here, never re-baselined
# upward.
crates/nika-compile/src/bindings.rs:bind
crates/nika-compile/src/cognition.rs:compile_with_cognition
crates/nika-compile/src/cognition/proposal.rs:merge
crates/nika-compile/src/cognition.rs:sampled
crates/nika-compile/src/compose.rs:feasibility
crates/nika-compile-reader/src/lexicon.rs:read_one
crates/nika-compile-reader/src/lexicon.rs:read
crates/nika-compile-reader/src/lexicon.rs:read_clause
crates/nika-compile/src/lib.rs:create
crates/nika-compile/src/predicate.rs:typed_rule
# rows · the harness adapter registry · nika-harness: one declarative row per adapter (its
# identities, handshake probe, version pin, auth probe, package) — a data table like the
# catalog's provenance table above, read top to bottom; it grows by one row per adapter.
crates/nika-harness/src/registry.rs:rows
