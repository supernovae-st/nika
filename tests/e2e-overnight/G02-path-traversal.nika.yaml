schema: "nika/workflow@0.12"
workflow: g02-path-traversal
description: "SECURITY: Path traversal attempt — MUST FAIL"
provider: mock

tasks:
  - id: traversal
    invoke:
      tool: "nika:read"
      params:
        path: "../../etc/passwd"
