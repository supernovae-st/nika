# @supernovae-st/nika-sdk (DEPRECATED)

> **This package is deprecated.** Use [`@supernovae-st/nika-client`](https://github.com/supernovae-st/nika-client) instead.

The TypeScript client (`nika-client`) is the canonical Node.js/TypeScript SDK:

- Pure TypeScript, zero native dependencies
- Works on Node.js, Deno, Bun, Cloudflare Workers
- Built-in retry, streaming (SSE), and webhook support
- Correct TypeScript types (no camelCase/.d.ts mismatch)

```bash
npm install @supernovae-st/nika-client
```
