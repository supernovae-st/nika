# COMBO: retry config on infer task
schema: nika/workflow@0.12
model: gpt-4.1-mini
workflow: combo-retry-infer

tasks:
  - id: flaky_infer
    provider: openai
    infer:
      prompt: "Say 'hello' and nothing else."
      max_tokens: 50
    retry:
      max_attempts: 3
      delay_ms: 1000
