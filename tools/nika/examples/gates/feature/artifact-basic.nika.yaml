# FEATURE: artifact: true (simple boolean saves output to default path)
schema: nika/workflow@0.12
model: gpt-4.1-mini
workflow: feat-artifact-basic
artifacts:
  dir: ./examples/gates/.test-output/artifacts
tasks:
  - id: gen
    infer: "Say hello in one word"
    provider: openai
    
    max_tokens: 50
    artifact: true
