schema: nika/workflow@0.12
workflow: feat-for-each-exec-parallel
tasks:
  - id: parallel_loop
    for_each: ["alpha", "beta", "gamma", "delta"]
    concurrency: 4
    exec: "echo processing {{with.item}}"
