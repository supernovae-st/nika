# FEATURE: retry with exponential backoff
schema: nika/workflow@0.12
workflow: feat-retry-backoff
tasks:
  - id: flaky
    fetch:
      url: "https://httpbin.org/get"
      method: GET
    retry:
      max_attempts: 3
      delay_ms: 100
      backoff: 2.0
