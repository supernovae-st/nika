schema: nika/workflow@0.12
workflow: feat-transform-default
tasks:
  - id: gen
    exec: "echo hello"
  - id: use
    with:
      val: $gen | default(fallback)
    exec:
      command: "echo Default is {{with.val}}"
      shell: true
    flow: [gen]
