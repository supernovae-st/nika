schema: nika/workflow@0.12
workflow: feat-transform-keys
tasks:
  - id: gen
    exec:
      command: "echo '{\"a\":1,\"b\":2}'"
      shell: true
  - id: use
    with:
      val: $gen | parse_json | keys
    exec:
      command: "echo Keys are {{with.val}}"
      shell: true
    depends_on: [gen]
