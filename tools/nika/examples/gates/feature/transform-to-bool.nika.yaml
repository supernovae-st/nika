schema: nika/workflow@0.12
workflow: feat-transform-to-bool
tasks:
  - id: gen
    exec: "echo true"
  - id: use
    with:
      val: $gen | to_bool
    exec:
      command: "echo Bool is {{with.val}}"
      shell: true
    depends_on: [gen]
