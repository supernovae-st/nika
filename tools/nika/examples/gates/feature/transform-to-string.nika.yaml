schema: nika/workflow@0.12
workflow: feat-transform-to-string
tasks:
  - id: gen
    exec: "echo 42"
  - id: use
    with:
      val: $gen | to_string
    exec:
      command: "echo String is {{with.val}}"
      shell: true
    flow: [gen]
