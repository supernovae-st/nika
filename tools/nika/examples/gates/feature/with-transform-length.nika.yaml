schema: nika/workflow@0.12
workflow: feat-with-transform-length
tasks:
  - id: step1
    exec: "echo hello"
  - id: step2
    with:
      data: $step1 | length
    exec:
      command: "echo {{with.data}}"
      shell: true
    flow: [step1]
