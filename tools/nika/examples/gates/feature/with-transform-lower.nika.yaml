schema: nika/workflow@0.12
workflow: feat-with-transform-lower
tasks:
  - id: step1
    exec: "echo HELLO"
  - id: step2
    with:
      data: $step1 | lower
    exec:
      command: "echo {{with.data}}"
      shell: true
    flow: [step1]
