schema: nika/workflow@0.12
workflow: feat-with-transform-upper
tasks:
  - id: step1
    exec: "echo hello"
  - id: step2
    with:
      data: $step1 | upper
    exec:
      command: "echo {{with.data}}"
      shell: true
    depends_on: [step1]
