# Test: bindings/05-inputs
# Tests: Workflow inputs binding
# Expected: Input parameters are accessible in tasks

schema: "nika/workflow@0.12"
workflow: test-bindings-inputs
description: "Test workflow inputs binding"

provider: claude

inputs:
  name: "TestUser"
  count: 5
  enabled: true

tasks:
  - id: use_inputs
    description: "Access workflow inputs"
    infer: "Hello {{inputs.name}}, processing {{inputs.count}} items."
