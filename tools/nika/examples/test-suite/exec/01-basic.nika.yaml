# Test: exec/01-basic
# Tests: Basic exec verb with shorthand command syntax
# Expected: Command executes and returns stdout

schema: "nika/workflow@0.12"
workflow: test-exec-basic
description: "Test basic exec verb with shorthand command"

provider: claude

tasks:
  - id: simple_exec
    description: "Simple command execution using shorthand"
    exec: "echo 'Hello from Nika'"
