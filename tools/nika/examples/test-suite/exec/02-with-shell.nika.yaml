# Test: exec/02-with-shell
# Tests: Exec verb with shell mode for pipes and redirects
# Expected: Shell features (pipes, redirects) work correctly

schema: "nika/workflow@0.10"
workflow: test-exec-with-shell
description: "Test exec verb with shell mode for pipes"

provider: mock

tasks:
  - id: with_pipe
    description: "Command with pipe requires shell mode"
    exec:
      command: "echo 'line1\nline2\nline3' | head -2"
      shell: true
