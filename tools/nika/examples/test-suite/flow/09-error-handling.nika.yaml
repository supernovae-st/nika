# Test: flow/09-error-handling
# Tests: DAG with error-handling-style dependencies
# Expected: All tasks succeed (no actual error triggered)
# Note: This tests DAG parsing only — the "risky" task always succeeds

schema: "nika/workflow@0.12"
workflow: test-flow-error-handling
description: "Test error-handling DAG shape (parse only)"

provider: claude

tasks:
  - id: risky_task
    description: "Task labeled risky (always succeeds)"
    exec: "echo 'Risky operation'"

  - id: dependent_task
    description: "Depends on risky task"
    exec: "echo 'After risky'"
    depends_on: [risky_task]

  - id: independent_task
    description: "Independent of risky task"
    exec: "echo 'Independent'"
