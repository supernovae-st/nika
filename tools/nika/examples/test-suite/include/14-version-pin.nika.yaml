# Test: include/14-version-pin
# Tests: Version pinning in includes (via path for now)
# Expected: Versioned includes work

schema: "nika/workflow@0.10"
workflow: test-include-version-pin
description: "Test versioned includes"

provider: claude

# Version pinning would use pkg:@scope/name@version/path
# Using path for test compatibility
include:
  - path: ./partials/setup.nika.yaml
    prefix: v1_

tasks:
  - id: use_versioned
    description: "Use versioned include"
    exec: "echo 'Version pin OK'"
    flow: [v1_validate]
