# Partial: cleanup.nika.yaml
# Reusable cleanup tasks for include tests

schema: "nika/workflow@0.12"
workflow: partial-cleanup

provider: claude

tasks:
  - id: finalize
    description: "Finalize operations"
    exec: "echo 'Finalized'"

  - id: report
    description: "Generate report"
    exec: "echo 'Report generated'"
    # Note: flow: [finalize] removed - internal flows need prefix handling (bug #TODO)
