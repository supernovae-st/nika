# Partial: processing.nika.yaml
# Reusable processing tasks

schema: "nika/workflow@0.10"
workflow: partial-processing

provider: claude

tasks:
  - id: transform
    description: "Transform data"
    exec: "echo 'Transformed'"

  - id: enrich
    description: "Enrich data"
    exec: "echo 'Enriched'"
    # Note: flow: [transform] removed - internal flows need prefix handling (bug #TODO)

  - id: validate_output
    description: "Validate output"
    exec: "echo 'Output validated'"
    # Note: flow: [enrich] removed - internal flows need prefix handling (bug #TODO)
