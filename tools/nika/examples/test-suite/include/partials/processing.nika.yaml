# Partial: processing.nika.yaml
# Reusable processing tasks

schema: "nika/workflow@0.12"
workflow: partial-processing

provider: claude

tasks:
  - id: transform
    description: "Transform data"
    exec: "echo 'Transformed'"

  - id: enrich
    description: "Enrich data"
    exec: "echo 'Enriched'"
    # Note: depends_on: [transform] removed - internal deps need prefix handling (bug #TODO)

  - id: validate_output
    description: "Validate output"
    exec: "echo 'Output validated'"
    # Note: depends_on: [enrich] removed - internal deps need prefix handling (bug #TODO)
