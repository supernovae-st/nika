# Partial: setup.nika.yaml
# Reusable setup tasks for include tests

schema: "nika/workflow@0.10"
workflow: partial-setup

provider: claude

tasks:
  - id: init
    description: "Initialize environment"
    exec: "echo 'Initialized'"

  - id: validate
    description: "Validate configuration"
    exec: "echo 'Validated'"
    # Note: flow: [init] removed - internal flows need prefix handling (bug #TODO)
