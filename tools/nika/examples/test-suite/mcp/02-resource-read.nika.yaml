# Test: mcp/02-resource-read
# Tests: MCP resource reading
# Expected: Resource content is retrieved

schema: "nika/workflow@0.10"
workflow: test-mcp-resource
description: "Test MCP resource reading"

provider: claude

mcp:
  resource-server:
    command: echo
    args: ["not used"]

tasks:
  - id: read_resource
    description: "Read MCP resource"
    invoke:
      mcp: resource-server
      resource: "file:///tmp/test-resource.txt"
