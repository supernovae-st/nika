# Sub-workflow to be called by nika:run
# Returns a simple result

schema: nika/workflow@0.9
workflow: sub-workflow

tasks:
  - id: sub_task
    infer: |
      This is the sub-workflow.
      Return EXACTLY: "SUB_RESULT_OK"
