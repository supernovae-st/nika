# Test: Basic artifact writing
# Expected: File is created with task output

schema: nika/workflow@0.9
workflow: test-basic-artifact
provider: openai

artifacts:
  base_path: ./test-audit/artifacts

tasks:
  - id: generate_content
    infer: |
      Return EXACTLY: "Hello from artifact test"
    artifact:
      path: "basic-test.txt"

  - id: verify
    depends_on: [generate_content]
    infer: |
      Artifact should be written to ./test-audit/artifacts/basic-test.txt
      Return "PASS" to confirm task completed.
